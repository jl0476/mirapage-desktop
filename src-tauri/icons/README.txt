// placeholder Tauri icons
//
// 真实图标需替换(1024x1024 主图 + 各平台尺寸)。
// Phase 9 占位:让 `cargo build --release` 与 CI 打包仍能通过。
//
// 替换方法:
//   1. 把 PNG / ICO / ICNS 真实文件放入此目录
//   2. 同步更新 `tauri.conf.json: bundle.icon` 路径(目前 32x32 / 128x128 / 128x128@2x / icon.icns / icon.ico)
//
// 当前 git 仓库的 icon 文件名占位:
32x32.png
128x128.png
128x128@2x.png
icon.icns
icon.ico
