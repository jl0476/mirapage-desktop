# v0.1.0-module3.0 书库 / 阅览记录 / directory_sort 重写 — 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** Library 视图只显示手动加入的书（`is_favorite=1`），History 视图按 Android BrowseHistory 重写为 folder-level（FileBrowser 自动记录），directory_sort 按 Android DirectorySortEntity 落地（per-folder 排序记忆）。`book` 表重命名为 `library` + 7 列新增，与 Android `LibraryEntity` 字节级对齐。

**架构：** Migration 005 一次性完成 schema 改造（book→library 改名 + 7 列 + browse_history 重建 + directory_sort 新建）→ Rust commands（library / history / directory_sort）→ 前端 tauri.ts 桥接 + 4 个 Pinia store（fileBrowser / history / directorySort / Library）→ useReaderActions（favorite 参数化 + 封面 enumerate）→ 视图重写（History.vue / Library.vue docstring）+ i18n 术语（zh-CN.ts 书架→书库）+ CLAUDE.md §6 决策记录。

**技术栈：** Rust（rusqlite Migration 005 + tauri::command）+ Vue 3 `<script setup>` + Pinia setup store + vue-i18n + happy-dom + Vitest

---

## 前置状态（必读 — 避免 Task 5 偏移）

v0.1.0-module2.1 完成后源码真实状态：

- **DB schema**：`book` / `progress` / `bookmark` / `like` / `browse_history` / `account` / `tag` / `book_tag` / `shortcut` / `settings` / `directory_sort` (migration 005 即将新建)
- **browse_history schema**：`book_id PRIMARY KEY`（per-book 旧 schema；本任务改成 per-folder）
- **book 表 5 列**：`id / title / source_descriptor / last_read_at / is_favorite`（本任务改成 11 列 + 重命名 library）
- **commands**：`library` (list_library / create_book / set_favorite) + `history` (list_history / record_history) + `progress`（含 `DELETE FROM browse_history`）+ `file_browser` / `bookmarks` / `find_next_volume` / `likes` / `search` / `settings` / `tags` / `shortcuts`
- **`lib/tauri.ts`**：`createBook(title, descriptor)` 2 参；`listHistory()` 返 `HistoryItem[]`（bookId / sourceDescriptor / lastPage / lastReadAt）；`recordHistory(descriptor, bookId, lastPage)` 3 参
- **stores**：`library` (items + sorted + favorites + refresh + toggleFavorite) + `history` (items + sorted + refresh + record + lastPageOf) + `fileBrowser`（已有 sortField / sortAscending 持久化到 settings store）+ `reader` / `slideshow` / `settings` / `bookmarks` / `likes` / `shortcuts` / `search` / `tags` / `readStatus`
- **composables**：`useReaderActions`（readNow / addToLibrary / ensureBookId 调 `createBook` + `recordHistory`）
- **views**：`History.vue`（**BROKEN**：3 处 RouterLink 用 `{ path: '/reader', query: { bookId } }` 不匹配 `/reader/:bookId`）+ `Library.vue`（顶部 docstring 写"书架视图"）
- **locales**：`zh-CN.ts` / `en-US.ts` — i18n key 是 `library.*` / `history.*`（display 字符串部分用了"书架"）
- **lib.rs 注册 commands**：list_library / create_book / set_favorite / list_history / record_history / 等等（不含 delete_history / directory_sort 系列）

**每个任务 brief 都以"前置任务 commit + 当前状态"为起点**——不假设其它任务已完成。

---

## 任务 1：Migration 005 — schema 改造

**文件：**
- 修改：`src-tauri/src/db/migrations.rs`

- [ ] **步骤 1：在 `migrations.rs::run` 末尾加 if current < 5 分支**

定位到现有 `if current < 4 { ... }` 之后追加：

```rust
    if current < 5 {
        apply_005_library_history_redesign(conn)?;
        conn.execute(
            "INSERT INTO _migrations (version, applied_at) VALUES (5, ?1)",
            [chrono_now()],
        )?;
    }
```

- [ ] **步骤 2：实现 `apply_005_library_history_redesign` 函数**

在文件末尾添加：

```rust
/// Migration 005 — book → library 重命名 + 7 列新增（Android LibraryEntity 对齐）
/// + browse_history per-book → per-folder 重写（Android BrowseHistoryEntity 对齐）
/// + directory_sort 新建（Android DirectorySortEntity 对齐）
fn apply_005_library_history_redesign(conn: &Connection) -> anyhow::Result<()> {
    conn.execute_batch(
        r#"
        -- 1. book → library 重命名
        ALTER TABLE book RENAME TO library;

        -- 2. library 补字段（Android LibraryEntity 11 列对齐）
        ALTER TABLE library ADD COLUMN source_type TEXT NOT NULL DEFAULT 'Local';
        ALTER TABLE library ADD COLUMN absolute_path TEXT NOT NULL DEFAULT '';
        ALTER TABLE library ADD COLUMN cover_entry_path TEXT;
        ALTER TABLE library ADD COLUMN cover_entry_name TEXT;
        ALTER TABLE library ADD COLUMN page_count INTEGER NOT NULL DEFAULT 0;
        ALTER TABLE library ADD COLUMN added_at INTEGER NOT NULL DEFAULT 0;

        -- 3. UNIQUE 索引：(source_descriptor, absolute_path) 一书一行（Android 对齐）
        CREATE UNIQUE INDEX IF NOT EXISTS idx_library_source_path
            ON library(source_descriptor, absolute_path);

        -- 4. browse_history 重写：per-book → per-folder
        DROP TABLE IF EXISTS browse_history;
        CREATE TABLE browse_history (
          source_descriptor TEXT NOT NULL,
          rel_path TEXT NOT NULL,
          display_name TEXT NOT NULL,
          last_visited_at INTEGER NOT NULL,
          PRIMARY KEY (source_descriptor, rel_path)
        );
        CREATE INDEX idx_browse_history_last_visited
            ON browse_history(last_visited_at DESC);

        -- 5. directory_sort 新建（Android DirectorySortEntity 对齐）
        CREATE TABLE directory_sort (
          location_key TEXT PRIMARY KEY,
          sort_field TEXT NOT NULL,
          ascending INTEGER NOT NULL
        );
        "#,
    )?;
    Ok(())
}
```

- [ ] **步骤 3：cargo check 编译**

```bash
cd src-tauri && cargo check
```

预期：无错误；migration 005 编译通过。

- [ ] **步骤 4：Commit**

```bash
git add src-tauri/src/db/migrations.rs
git commit -m "feat(db): migration 005 — book→library + 7 列 + browse_history 重写 + directory_sort 新建"
```

---

## 任务 2：Rust library.rs — BookItem 扩展 + list_library WHERE filter

**文件：**
- 修改：`src-tauri/src/commands/library.rs`（BookItem struct + list_library 函数体）

- [ ] **步骤 1：BookItem 加 6 字段（与 Android LibraryEntity 对齐）**

定位到当前 `BookItem` 定义，替换为：

```rust
#[derive(Debug, Serialize, Clone)]
#[serde(rename_all = "camelCase")]
pub struct BookItem {
    pub id: i64,
    pub title: String,
    pub source_descriptor: String,
    pub source_type: String,
    pub absolute_path: String,
    pub cover_entry_path: Option<String>,
    pub cover_entry_name: Option<String>,
    pub page_count: i64,
    pub last_read_at: Option<i64>,
    pub added_at: i64,
    pub is_favorite: bool,
}
```

- [ ] **步骤 2：list_library 函数体改：表名 + 11 列 + WHERE filter**

```rust
#[tauri::command]
pub fn list_library(db: tauri::State<crate::db::Db>) -> Result<Vec<BookItem>, String> {
    let conn = db.conn();
    let mut stmt = conn
        .prepare(
            "SELECT id, title, source_descriptor, source_type, absolute_path,
                    cover_entry_path, cover_entry_name, page_count,
                    last_read_at, added_at, is_favorite
             FROM library
             WHERE is_favorite = 1
             ORDER BY last_read_at IS NULL, last_read_at DESC, added_at DESC",
        )
        .map_err(|e| e.to_string())?;
    let rows = stmt
        .query_map([], |row| {
            Ok(BookItem {
                id: row.get::<_, i64>(0)?,
                title: row.get::<_, String>(1)?,
                source_descriptor: row.get::<_, String>(2)?,
                source_type: row.get::<_, String>(3)?,
                absolute_path: row.get::<_, String>(4)?,
                cover_entry_path: row.get::<_, Option<String>>(5)?,
                cover_entry_name: row.get::<_, Option<String>>(6)?,
                page_count: row.get::<_, i64>(7)?,
                last_read_at: row.get::<_, Option<i64>>(8)?,
                added_at: row.get::<_, i64>(9)?,
                is_favorite: row.get::<_, i64>(10)? != 0,
            })
        })
        .map_err(|e| e.to_string())?
        .collect::<Result<Vec<_>, _>>()
        .map_err(|e| e.to_string())?;
    Ok(rows)
}
```

- [ ] **步骤 3：cargo check**

```bash
cd src-tauri && cargo check
```

预期：无错误。`set_favorite` 函数不动（表名 book → library 在 SQL 里，但 SQLite 对 `book` 不存在；改成 `library`）。

等等 — 当前 `set_favorite` 用了 `UPDATE book`！这个也会编译失败。**修正步骤 3**：把 set_favorite 的 SQL 也改成 `library`。

- [ ] **步骤 4：set_favorite 表名 book → library**

定位到 `set_favorite` 函数：

```rust
#[tauri::command]
pub fn set_favorite(
    book_id: i64,
    favorite: bool,
    db: tauri::State<crate::db::Db>,
) -> Result<(), String> {
    let conn = db.conn();
    conn.execute(
        "UPDATE library SET is_favorite = ?1 WHERE id = ?2",
        rusqlite::params![if favorite { 1i64 } else { 0i64 }, book_id],
    )
    .map_err(|e| e.to_string())?;
    Ok(())
}
```

- [ ] **步骤 5：cargo check + cargo test**

```bash
cd src-tauri && cargo check && cargo test -p mirapage-desktop-lib
```

预期：编译通过；既有 library 测试 0 fail。

- [ ] **步骤 6：Commit**

```bash
git add src-tauri/src/commands/library.rs
git commit -m "feat(library): BookItem 11 列对齐 Android + list_library WHERE is_favorite=1"
```

---

## 任务 3：Rust library.rs — create_book 入参扩展 + 复用升级逻辑

**文件：**
- 修改：`src-tauri/src/commands/library.rs`（CreateBookArgs + create_book 函数体）

- [ ] **步骤 1：CreateBookArgs 加 6 字段**

定位到当前 `CreateBookArgs`，替换为：

```rust
#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct CreateBookArgs {
    pub title: String,
    pub source_descriptor: serde_json::Value,
    pub absolute_path: String,
    pub source_type: String,
    pub favorite: bool,
    /// caller 已 enumerate 一次，传入封面 + 页数；enumerate 失败时传 None / 0
    pub cover_entry_path: Option<String>,
    pub cover_entry_name: Option<String>,
    pub page_count: i64,
}
```

- [ ] **步骤 2：create_book 重写为复用升级模式（Android importFromSource 行为）**

```rust
#[tauri::command]
pub fn create_book(args: CreateBookArgs, db: tauri::State<crate::db::Db>) -> Result<i64, String> {
    let conn = db.conn();
    let descriptor_str = serde_json::to_string(&args.source_descriptor).map_err(|e| e.to_string())?;

    // 复用同 (sourceDescriptor, absolutePath) 的 row
    let existing: Option<(i64, bool)> = conn
        .query_row(
            "SELECT id, is_favorite FROM library WHERE source_descriptor = ?1 AND absolute_path = ?2",
            rusqlite::params![descriptor_str, args.absolute_path],
            |row| Ok((row.get::<_, i64>(0)?, row.get::<_, i64>(1)? != 0)),
        )
        .ok();

    if let Some((id, is_fav)) = existing {
        // favorite=true 且当前未 favorite → 升级（Android LibraryRepository.importFromSource 行为）
        if args.favorite && !is_fav {
            conn.execute(
                "UPDATE library SET is_favorite = 1 WHERE id = ?1",
                rusqlite::params![id],
            )
            .map_err(|e| e.to_string())?;
        }
        return Ok(id);
    }

    // 不存在则 INSERT（11 列对齐 Android LibraryEntity）
    let now = chrono_now();
    conn.execute(
        "INSERT INTO library
            (title, source_descriptor, source_type, absolute_path,
             cover_entry_path, cover_entry_name, page_count,
             added_at, is_favorite)
         VALUES (?1, ?2, ?3, ?4, ?5, ?6, ?7, ?8, ?9)",
        rusqlite::params![
            args.title,
            descriptor_str,
            args.source_type,
            args.absolute_path,
            args.cover_entry_path,
            args.cover_entry_name,
            args.page_count,
            now,
            if args.favorite { 1i64 } else { 0i64 },
        ],
    )
    .map_err(|e| e.to_string())?;
    Ok(conn.last_insert_rowid())
}
```

注意：当前 `chrono_now` 在 `commands::progress` 等模块顶部已经定义过；如果 library.rs 引用不到，**先 import**。检查现有 `use` 语句，必要时加：

```rust
use crate::chrono_now;  // 或 use crate::commands::progress::chrono_now; 视乎现有 import
```

（视乎现有 `commands/mod.rs` 暴露形式而定；以 `cargo check` 不报错为准）

- [ ] **步骤 3：cargo check**

```bash
cd src-tauri && cargo check
```

预期：无错误。

- [ ] **步骤 4：Commit**

```bash
git add src-tauri/src/commands/library.rs
git commit -m "feat(library): create_book 入参扩展 (favorite + absolutePath + 封面 + 页数) + 复用升级逻辑"
```

---

## 任务 4：Rust history.rs — 完全重写为 folder-level

**文件：**
- 重写：`src-tauri/src/commands/history.rs`

- [ ] **步骤 1：替换 history.rs 全部内容**

```rust
//! `commands::history` —— 阅览记录 (folder-level, Android BrowseHistory 对齐)

use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Clone)]
#[serde(rename_all = "camelCase")]
pub struct BrowseHistoryEntry {
    pub source_descriptor: serde_json::Value,
    pub rel_path: String,
    pub display_name: String,
    pub last_visited_at: i64,
}

#[tauri::command]
pub fn list_history(db: tauri::State<crate::db::Db>) -> Result<Vec<BrowseHistoryEntry>, String> {
    let conn = db.conn();
    let mut stmt = conn
        .prepare(
            "SELECT source_descriptor, rel_path, display_name, last_visited_at
             FROM browse_history ORDER BY last_visited_at DESC",
        )
        .map_err(|e| e.to_string())?;
    let rows = stmt
        .query_map([], |row| {
            let sd_str: String = row.get(0)?;
            let sd_value: serde_json::Value = serde_json::from_str(&sd_str)
                .unwrap_or(serde_json::Value::Null);
            Ok(BrowseHistoryEntry {
                source_descriptor: sd_value,
                rel_path: row.get(1)?,
                display_name: row.get(2)?,
                last_visited_at: row.get(3)?,
            })
        })
        .map_err(|e| e.to_string())?
        .collect::<Result<Vec<_>, _>>()
        .map_err(|e| e.to_string())?;
    Ok(rows)
}

#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RecordHistoryArgs {
    pub source_descriptor: serde_json::Value,
    pub rel_path: String,
    pub display_name: String,
}

#[tauri::command]
pub fn record_history(args: RecordHistoryArgs, db: tauri::State<crate::db::Db>) -> Result<(), String> {
    let conn = db.conn();
    let descriptor_str = serde_json::to_string(&args.source_descriptor).map_err(|e| e.to_string())?;
    let now = chrono_now();
    conn.execute(
        "INSERT INTO browse_history (source_descriptor, rel_path, display_name, last_visited_at)
         VALUES (?1, ?2, ?3, ?4)
         ON CONFLICT(source_descriptor, rel_path) DO UPDATE SET
           display_name = excluded.display_name,
           last_visited_at = excluded.last_visited_at",
        rusqlite::params![descriptor_str, args.rel_path, args.display_name, now],
    )
    .map_err(|e| e.to_string())?;
    Ok(())
}

#[tauri::command]
pub fn delete_history(
    source_descriptor: serde_json::Value,
    rel_path: String,
    db: tauri::State<crate::db::Db>,
) -> Result<(), String> {
    let conn = db.conn();
    let descriptor_str = serde_json::to_string(&source_descriptor).map_err(|e| e.to_string())?;
    conn.execute(
        "DELETE FROM browse_history WHERE source_descriptor = ?1 AND rel_path = ?2",
        rusqlite::params![descriptor_str, rel_path],
    )
    .map_err(|e| e.to_string())?;
    Ok(())
}

fn chrono_now() -> i64 {
    std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .map(|d| d.as_secs() as i64)
        .unwrap_or(0)
}
```

- [ ] **步骤 2：cargo check**

```bash
cd src-tauri && cargo check
```

预期：无错误。`HistoryItem` / `RecordHistoryArgs (旧)` 类型不再导出，下游 `lib/tauri.ts` 同步更新（任务 7 覆盖）。

- [ ] **步骤 3：Commit**

```bash
git add src-tauri/src/commands/history.rs
git commit -m "feat(history): 重写为 folder-level (Android BrowseHistoryEntity 对齐) + delete_history"
```

---

## 任务 5：Rust directory_sort.rs — 新建

**文件：**
- 新建：`src-tauri/src/commands/directory_sort.rs`

- [ ] **步骤 1：创建文件**

```rust
//! `commands::directory_sort` —— 按文件夹排序覆盖（Android DirectorySortEntity 对齐）

use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Clone)]
#[serde(rename_all = "camelCase")]
pub struct DirectorySort {
    pub location_key: String,
    pub sort_field: String,
    pub ascending: bool,
}

/// locationKey = SourceDescriptorJson + "|" + relPath（与 Android DirectorySortRepository 一致）
fn location_key_of(source_descriptor: &serde_json::Value, rel_path: &str) -> String {
    let sd_str = serde_json::to_string(source_descriptor).unwrap_or_default();
    format!("{}|{}", sd_str, rel_path)
}

#[tauri::command]
pub fn get_directory_sort(
    source_descriptor: serde_json::Value,
    rel_path: String,
    db: tauri::State<crate::db::Db>,
) -> Result<Option<DirectorySort>, String> {
    let conn = db.conn();
    let key = location_key_of(&source_descriptor, &rel_path);
    let mut stmt = conn
        .prepare(
            "SELECT location_key, sort_field, ascending
             FROM directory_sort WHERE location_key = ?1 LIMIT 1",
        )
        .map_err(|e| e.to_string())?;
    let mut rows = stmt.query([&key]).map_err(|e| e.to_string())?;
    if let Some(row) = rows.next().map_err(|e| e.to_string())? {
        Ok(Some(DirectorySort {
            location_key: row.get(0).map_err(|e| e.to_string())?,
            sort_field: row.get(1).map_err(|e| e.to_string())?,
            ascending: row.get::<_, i64>(2).map_err(|e| e.to_string())? != 0,
        }))
    } else {
        Ok(None)
    }
}

#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct SetDirectorySortArgs {
    pub source_descriptor: serde_json::Value,
    pub rel_path: String,
    pub sort_field: String,
    pub ascending: bool,
}

#[tauri::command]
pub fn set_directory_sort(
    args: SetDirectorySortArgs,
    db: tauri::State<crate::db::Db>,
) -> Result<(), String> {
    let conn = db.conn();
    let key = location_key_of(&args.source_descriptor, &args.rel_path);
    conn.execute(
        "INSERT INTO directory_sort (location_key, sort_field, ascending)
         VALUES (?1, ?2, ?3)
         ON CONFLICT(location_key) DO UPDATE SET
           sort_field = excluded.sort_field,
           ascending = excluded.ascending",
        rusqlite::params![
            key,
            args.sort_field,
            if args.ascending { 1i64 } else { 0i64 },
        ],
    )
    .map_err(|e| e.to_string())?;
    Ok(())
}
```

- [ ] **步骤 2：在 commands/mod.rs 注册新模块**

定位到 `src-tauri/src/commands/mod.rs`，找到现有 module 声明（如 `pub mod library;`），追加：

```rust
pub mod directory_sort;
```

- [ ] **步骤 3：cargo check**

```bash
cd src-tauri && cargo check
```

预期：无错误。

- [ ] **步骤 4：Commit**

```bash
git add src-tauri/src/commands/directory_sort.rs src-tauri/src/commands/mod.rs
git commit -m "feat(directory_sort): 新建 + get_directory_sort + set_directory_sort（Android 对齐）"
```

---

## 任务 6：Rust progress.rs 清理 — 删 DELETE FROM browse_history

**文件：**
- 修改：`src-tauri/src/commands/progress.rs`

- [ ] **步骤 1：grep 当前引用**

```bash
cd src-tauri && grep -n "browse_history\|book" src/commands/progress.rs
```

预期：找出所有 `DELETE FROM browse_history` / `book` 引用。

- [ ] **步骤 2：删除 `DELETE FROM browse_history WHERE book_id = ?1`**

定位到每个匹配，**整行删除**（包括上下文 SQL 块）。具体行号视乎当前实现 — 关键点是 `browse_history` 这张表已经不再 per-book（migration 005 改成 per-folder），所以 `book_id` 列在新表里不存在；任何 `DELETE FROM browse_history WHERE book_id = ?1` 都是死代码。

- [ ] **步骤 3：bookmark / progress / save_progress 里残留的 `book` 表名引用 → `library`**

凡是 `UPDATE book` / `FROM book` / `INSERT INTO book` 都改成 `library`。

- [ ] **步骤 4：cargo check + cargo test**

```bash
cd src-tauri && cargo check && cargo test -p mirapage-desktop-lib
```

预期：编译通过；progress 测试 0 fail。

- [ ] **步骤 5：Commit**

```bash
git add src-tauri/src/commands/progress.rs
git commit -m "refactor(progress): 删 DELETE FROM browse_history + book→library 表名"
```

---

## 任务 7：Rust lib.rs — 注册新 commands

**文件：**
- 修改：`src-tauri/src/lib.rs`（`tauri::generate_handler!` 列表）

- [ ] **步骤 1：grep 当前注册**

```bash
cd src-tauri && grep -n "commands::history::\|commands::library::" src/lib.rs
```

预期：当前注册了 `list_history` / `record_history`（无 `delete_history`）+ `list_library` / `create_book` / `set_favorite`。

- [ ] **步骤 2：替换注册块**

定位到 `generate_handler!` 列表的 `library` / `history` 段落，替换为：

```rust
            commands::library::list_library,
            commands::library::create_book,
            commands::library::set_favorite,
            commands::history::list_history,
            commands::history::record_history,
            commands::history::delete_history,
            commands::directory_sort::get_directory_sort,
            commands::directory_sort::set_directory_sort,
```

- [ ] **步骤 3：cargo check**

```bash
cd src-tauri && cargo check
```

预期：无错误；所有 command 注册成功。

- [ ] **步骤 4：Commit**

```bash
git add src-tauri/src/lib.rs
git commit -m "feat(lib): 注册 delete_history + get/set_directory_sort"
```

---

## 任务 8：Frontend tauri.ts — IPC 签名扩展

**文件：**
- 修改：`src/lib/tauri.ts`（HistoryItem → BrowseHistoryEntry；createBook 新签名；新增 deleteHistory / getDirectorySort / setDirectorySort）

- [ ] **步骤 1：删除旧 `HistoryItem` interface**

定位 `export interface HistoryItem { ... }` —— 整段删除。

- [ ] **步骤 2：新增 `BrowseHistoryEntry` interface**

在 lib/tauri.ts 顶部 import 区附近添加：

```ts
export interface BrowseHistoryEntry {
  sourceDescriptor: SourceDescriptor;
  relPath: string;
  displayName: string;
  lastVisitedAt: number;
}
```

- [ ] **步骤 3：替换 `createBook` 签名**

定位 `export async function createBook(...)`，替换为：

```ts
export interface CreateBookArgs {
  title: string;
  sourceDescriptor: SourceDescriptor;
  absolutePath: string;
  sourceType: string;
  favorite: boolean;
  coverEntryPath: string | null;
  coverEntryName: string | null;
  pageCount: number;
}
export async function createBook(args: CreateBookArgs): Promise<number> {
  return invoke<number>('create_book', {
    args: {
      title: args.title,
      sourceDescriptor: args.sourceDescriptor,
      absolutePath: args.absolutePath,
      sourceType: args.sourceType,
      favorite: args.favorite,
      coverEntryPath: args.coverEntryPath,
      coverEntryName: args.coverEntryName,
      pageCount: args.pageCount,
    },
  });
}
```

- [ ] **步骤 4：替换 `listHistory` + `recordHistory` + 新增 `deleteHistory`**

定位旧实现，替换为：

```ts
export async function listHistory(): Promise<BrowseHistoryEntry[]> {
  return invoke<BrowseHistoryEntry[]>('list_history');
}

export async function recordHistory(
  sourceDescriptor: SourceDescriptor,
  relPath: string,
  displayName: string,
): Promise<void> {
  await invoke<void>('record_history', {
    args: { sourceDescriptor, relPath, displayName },
  });
}

export async function deleteHistory(
  sourceDescriptor: SourceDescriptor,
  relPath: string,
): Promise<void> {
  await invoke<void>('delete_history', { sourceDescriptor, relPath });
}
```

- [ ] **步骤 5：新增 `getDirectorySort` + `setDirectorySort`**

```ts
export interface DirectorySort {
  locationKey: string;
  sortField: 'name' | 'modifiedAt' | 'size';
  ascending: boolean;
}

export async function getDirectorySort(
  sourceDescriptor: SourceDescriptor,
  relPath: string,
): Promise<DirectorySort | null> {
  return invoke<DirectorySort | null>('get_directory_sort', { sourceDescriptor, relPath });
}

export async function setDirectorySort(
  sourceDescriptor: SourceDescriptor,
  relPath: string,
  sortField: 'name' | 'modifiedAt' | 'size',
  ascending: boolean,
): Promise<void> {
  await invoke<void>('set_directory_sort', {
    args: { sourceDescriptor, relPath, sortField, ascending },
  });
}
```

注意：sortField 字符串对齐现有 `SortField` 类型（`'name' | 'modifiedAt' | 'size'`）。

- [ ] **步骤 6：npm run type-check**

```bash
npm run type-check
```

预期：无错误；类型层级一致。

- [ ] **步骤 7：Commit**

```bash
git add src/lib/tauri.ts
git commit -m "feat(tauri): createBook 11 字段 + BrowseHistoryEntry 重命名 + 新增 deleteHistory + directory_sort IPC"
```

---

## 任务 9：Frontend history.ts — store 重写

**文件：**
- 重写：`src/stores/history.ts`

- [ ] **步骤 1：替换 history.ts 全部内容**

```ts
/**
 * History Pinia store
 * 与 Rust commands::history 对接 (v0.1.0-module3.0):
 *   - listHistory() → BrowseHistoryEntry[]  (folder-level, Android BrowseHistory 对齐)
 *   - recordHistory(source, relPath, displayName)  (FileBrowser 导航成功后调)
 *   - deleteHistory(source, relPath)
 */
import { defineStore } from 'pinia';
import { ref } from 'vue';
import { listHistory, recordHistory, deleteHistory, type BrowseHistoryEntry } from '@/lib/tauri';

export const useHistoryStore = defineStore('history', () => {
  const items = ref<BrowseHistoryEntry[]>([]);

  async function refresh(): Promise<void> {
    items.value = await listHistory();
  }

  /** FileBrowser.fetch 成功后调 — 容错，失败不抛 */
  async function record(
    sourceDescriptor: BrowseHistoryEntry['sourceDescriptor'],
    relPath: string,
    displayName: string,
  ): Promise<void> {
    try {
      await recordHistory(sourceDescriptor, relPath, displayName);
    } catch (e) {
      // 容错：历史记录失败不应影响 FileBrowser 列表展示
      console.warn('[history] record failed', e);
    }
  }

  async function deleteEntry(entry: BrowseHistoryEntry): Promise<void> {
    await deleteHistory(entry.sourceDescriptor, entry.relPath);
    await refresh();
  }

  return { items, refresh, record, deleteEntry };
});
```

- [ ] **步骤 2：npm run type-check**

```bash
npm run type-check
```

预期：无错误。

- [ ] **步骤 3：Commit**

```bash
git add src/stores/history.ts
git commit -m "feat(history): store 重写为 BrowseHistoryEntry + record/deleteEntry"
```

---

## 任务 10：Frontend directorySort.ts — 新建 store

**文件：**
- 新建：`src/stores/directorySort.ts`

- [ ] **步骤 1：创建文件**

```ts
/**
 * directorySort Pinia store — v0.1.0-module3.0
 * per-folder 排序覆盖（Android DirectorySortRepository 对齐）
 *
 * locationKey = JSON.stringify(sourceDescriptor) + "|" + relPath
 * 用户改排序时调 set() 写 override；下次进同文件夹 fetch() 读 override
 */
import { defineStore } from 'pinia';
import { ref } from 'vue';
import { getDirectorySort, setDirectorySort, type SourceDescriptor } from '@/lib/tauri';

export type DirectorySortOverride = {
  sortField: 'name' | 'modifiedAt' | 'size';
  ascending: boolean;
};

export const useDirectorySortStore = defineStore('directorySort', () => {
  const overrides = ref<Map<string, DirectorySortOverride>>(new Map());

  function keyOf(sourceDescriptor: SourceDescriptor, relPath: string): string {
    return JSON.stringify(sourceDescriptor) + '|' + relPath;
  }

  /** 读 override；未命中返回 null，调用方 fallback 到 settings 默认 */
  async function resolve(
    sourceDescriptor: SourceDescriptor,
    relPath: string,
  ): Promise<DirectorySortOverride | null> {
    const key = keyOf(sourceDescriptor, relPath);
    if (overrides.value.has(key)) {
      return overrides.value.get(key) ?? null;
    }
    try {
      const result = await getDirectorySort(sourceDescriptor, relPath);
      if (!result) return null;
      const ov: DirectorySortOverride = {
        sortField: result.sortField,
        ascending: result.ascending,
      };
      overrides.value.set(key, ov);
      return ov;
    } catch {
      return null;
    }
  }

  /** 写 override（用户在该文件夹改了排序时调） */
  async function set(
    sourceDescriptor: SourceDescriptor,
    relPath: string,
    sort: DirectorySortOverride,
  ): Promise<void> {
    const key = keyOf(sourceDescriptor, relPath);
    overrides.value.set(key, sort);
    try {
      await setDirectorySort(
        sourceDescriptor,
        relPath,
        sort.sortField,
        sort.ascending,
      );
    } catch (e) {
      console.warn('[directorySort] set failed', e);
    }
  }

  return { resolve, set };
});
```

- [ ] **步骤 2：npm run type-check**

```bash
npm run type-check
```

预期：无错误。

- [ ] **步骤 3：Commit**

```bash
git add src/stores/directorySort.ts
git commit -m "feat(directorySort): store 新建 (per-folder sort override)"
```

---

## 任务 11：Frontend fileBrowser.ts — 集成 directorySort + 自动 recordHistory

**文件：**
- 修改：`src/stores/fileBrowser.ts`

- [ ] **步骤 1：在 import 区添加新 import**

定位 `import { ... } from '@/lib/tauri';` 末尾，加：

```ts
import { useDirectorySortStore } from '@/stores/directorySort';
import { useSettingsStore } from '@/stores/settings';
```

如果 `import { useSettingsStore }` 已存在则跳过。

- [ ] **步骤 2：添加 effective sort refs**

定位 `const sortField = ref<SortField>('name');` 附近，**追加**：

```ts
  const effectiveSortField = ref<SortField>('name');
  const effectiveSortAscending = ref<boolean>(true);
```

- [ ] **步骤 3：`fetch` 函数体集成 directorySort resolve + recordHistory**

定位 `async function fetch(path: string) { ... }`，替换为：

```ts
  async function fetch(path: string): Promise<void> {
    if (rootPath.value === null) return;
    loading.value = true;
    error.value = null;
    log('[fileBrowser] fetch', { rootPath: rootPath.value, path });
    try {
      const descriptor = toDescriptor(rootPath.value);
      const result = await listDirectory(descriptor, path);
      log('[fileBrowser] listDirectory returned', result.length, 'entries');

      // ★ per-folder 排序覆盖 → fallback 到 settings 默认
      const dsStore = useDirectorySortStore();
      const settings = useSettingsStore();
      const override = await dsStore.resolve(descriptor, path).catch(() => null);
      effectiveSortField.value = override?.sortField ?? sortField.value;
      effectiveSortAscending.value = override?.ascending ?? sortAscending.value;

      entries.value = result;
      lastFetchedPath.value = path;

      // ★ 自动 upsert browse_history (Android BrowseHistoryRepository.record 行为)
      const { useHistoryStore } = await import('@/stores/history');
      const history = useHistoryStore();
      void history.record(descriptor, path, directoryDisplayName(path));
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      log('[fileBrowser] fetch error', msg);
      error.value = { kind: 'io', message: msg };
    } finally {
      loading.value = false;
    }
  }

  function directoryDisplayName(dir: string): string {
    if (!dir) return rootPath.value ?? 'root';
    const parts = dir.split(/[/\\]/).filter(Boolean);
    return parts[parts.length - 1] ?? 'root';
  }
```

（dynamic import `useHistoryStore` 避免循环依赖 — `history.ts` 不依赖 fileBrowser，OK；dynamic import 是为了避免 setup-time 循环）

- [ ] **步骤 4：`setSortField` 函数体加 directory_sort.set**

定位 `function setSortField(f: SortField): void { ... }`，**替换为**：

```ts
  function setSortField(f: SortField): void {
    if (sortField.value === f) {
      sortAscending.value = !sortAscending.value;
    } else {
      sortField.value = f;
    }
    persist('fb_sort_field', sortField.value);
    persist('fb_sort_ascending', sortAscending.value ? '1' : '0');
    effectiveSortField.value = f;
    effectiveSortAscending.value = sortAscending.value;

    // ★ 写 per-folder override（Android DirectorySortRepository.setSort 一致）
    if (rootPath.value !== null) {
      const descriptor = toDescriptor(rootPath.value);
      const dsStore = useDirectorySortStore();
      void dsStore.set(descriptor, currentPath.value, {
        sortField: f,
        ascending: sortAscending.value,
      });
    }
  }
```

- [ ] **步骤 5：`toggleSortOrder` 函数体加 directory_sort.set**

定位 `function toggleSortOrder()`, **替换为**：

```ts
  function toggleSortOrder(): void {
    sortAscending.value = !sortAscending.value;
    persist('fb_sort_ascending', sortAscending.value ? '1' : '0');
    effectiveSortAscending.value = sortAscending.value;
    if (rootPath.value !== null) {
      const descriptor = toDescriptor(rootPath.value);
      const dsStore = useDirectorySortStore();
      void dsStore.set(descriptor, currentPath.value, {
        sortField: sortField.value,
        ascending: sortAscending.value,
      });
    }
  }
```

- [ ] **步骤 6：`sortedEntries` computed 改用 effective 值**

定位：
```ts
  const sortedEntries = computed<MediaEntry[]>(() =>
    sortEntries(entries.value, sortField.value, sortAscending.value),
  );
```

替换为：

```ts
  const sortedEntries = computed<MediaEntry[]>(() =>
    sortEntries(entries.value, effectiveSortField.value, effectiveSortAscending.value),
  );
```

- [ ] **步骤 7：导出 effectiveSortField / effectiveSortAscending**

定位 `return {` 块，**追加**：

```ts
    effectiveSortField,
    effectiveSortAscending,
```

- [ ] **步骤 8：npm run type-check**

```bash
npm run type-check
```

预期：无错误。

- [ ] **步骤 9：Commit**

```bash
git add src/stores/fileBrowser.ts
git commit -m "feat(fileBrowser): 集成 directorySort (per-folder override) + 自动 recordHistory"
```

---

## 任务 12：Frontend useReaderActions.ts — 重写 ensureBookId + favorite 参数

**文件：**
- 修改：`src/composables/useReaderActions.ts`

- [ ] **步骤 1：替换 import 区**

定位 import 区，替换为：

```ts
import { useRouter } from 'vue-router';
import { createBook, listDirectory, type CreateBookArgs } from '@/lib/tauri';
import { log } from '@/lib/logger';
import { mimeUtils } from '@/lib/mime';
import { naturalCompare } from '@/lib/naturalSort';
import type { MediaEntry, SourceDescriptor } from '@/lib/sourceDescriptor';
import type { Router } from 'vue-router';
```

如果现有 import 不一样，按 `cargo check`/`tsc` 报错的实际项调整。

- [ ] **步骤 2：替换 `ensureBookId` 函数 + 加 enumerate**

定位 `async function ensureBookId(entry: MediaEntry): Promise<number | null>`，替换为：

```ts
  async function ensureBookId(entry: MediaEntry, favorite: boolean): Promise<number | null> {
    if (!entry.isDirectory) {
      log('[useReaderActions] ensureBookId: entry is not a directory', entry.name);
      return null;
    }
    const rootPath = opts.resolveRootPath(/* root, not entry */);  // 来自 opts
    const absPath = entry.path;
    const descriptor = opts.buildSourceDescriptor(rootPath);
    const sourceType = descriptor.type === 'local' ? 'Local' : capitalize(descriptor.type);

    const cover = await enumerateCover(descriptor, absPath);

    try {
      const args: CreateBookArgs = {
        title: entry.name,
        sourceDescriptor: descriptor,
        absolutePath: absPath,
        sourceType,
        favorite,
        ...cover,
      };
      const bookId = await createBook(args);
      log('[useReaderActions] createBook', favorite ? 'favorite=true' : 'favorite=false', '→ bookId', bookId);
      return bookId;
    } catch (e) {
      log('[useReaderActions] createBook failed', e);
      return null;
    }
  }

  async function enumerateCover(
    descriptor: SourceDescriptor,
    absPath: string,
  ): Promise<{ coverEntryPath: string | null; coverEntryName: string | null; pageCount: number }> {
    try {
      const entries = await listDirectory(descriptor, absPath);
      const images = entries
        .filter((e) => !e.isDirectory && mimeUtils.isImage(e.name))
        .sort((a, b) => naturalCompare(a.name, b.name));
      if (images.length === 0) {
        return { coverEntryPath: null, coverEntryName: null, pageCount: 0 };
      }
      const first = images[0]!;
      return {
        coverEntryPath: first.path,
        coverEntryName: first.name,
        pageCount: images.length,
      };
    } catch (e) {
      log('[useReaderActions] enumerateCover failed', e);
      return { coverEntryPath: null, coverEntryName: null, pageCount: 0 };
    }
  }

  function capitalize(s: string): string {
    return s.charAt(0).toUpperCase() + s.slice(1);
  }
```

- [ ] **步骤 3：`readNow` 传 favorite=false**

定位 `async function readNow(entry: MediaEntry): Promise<void>`，把 `ensureBookId(entry)` 改为 `ensureBookId(entry, false)`。

- [ ] **步骤 4：`addToLibrary` 传 favorite=true**

定位 `async function addToLibrary(entry: MediaEntry): Promise<void>`，把 `ensureBookId(entry)` 改为 `ensureBookId(entry, true)`。

- [ ] **步骤 5：删除 listHistory / recordHistory 调用**

当前 `ensureBookId` 旧实现里有 `listHistory().find(...)` + `recordHistory(...)` 的复用逻辑 —— 这些都需要**删除**。所有引用 `listHistory` / `recordHistory` 的代码移除（已删除后 import 会报错）。

- [ ] **步骤 6：npm run type-check**

```bash
npm run type-check
```

预期：无错误。

- [ ] **步骤 7：Commit**

```bash
git add src/composables/useReaderActions.ts
git commit -m "feat(readerActions): ensureBookId(favorite) + enumerate 封面 + 删 listHistory/recordHistory 调用"
```

---

## 任务 13：Frontend History.vue — 重写

**文件：**
- 重写：`src/views/History.vue`

- [ ] **步骤 1：替换 template + script**

```vue
<script setup lang="ts">
/**
 * HistoryView.vue — 阅览记录 (folder-level, Android BrowseHistory 对齐)
 * 列文件夹 + 时间；点击 → 跳回 FileBrowser 对应 root + path；右侧 X 删除
 */
import { onMounted } from 'vue';
import { useRouter } from 'vue-router';
import { storeToRefs } from 'pinia';
import { useI18n } from 'vue-i18n';
import { useHistoryStore } from '@/stores/history';
import { useFileBrowserStore } from '@/stores/fileBrowser';
import { formatDate } from '@/locales/helpers';

const { t } = useI18n();
const router = useRouter();
const store = useHistoryStore();
const { items } = storeToRefs(store);
const fb = useFileBrowserStore();

onMounted(() => {
  store.refresh();
});

async function openEntry(entry: BrowseHistoryEntry) {
  const sd = entry.sourceDescriptor;
  if (sd.type !== 'local') return;  // Phase 1 仅支持 Local
  await fb.setRoot(sd.rootPath);
  if (entry.relPath) {
    await fb.navigate(entry.relPath);
  }
  router.push({ name: 'home' });
}

async function removeEntry(entry: BrowseHistoryEntry) {
  await store.deleteEntry(entry);
}
</script>

<template>
  <main class="history-view">
    <header class="history-header">
      <h2>{{ t('history.title') }}</h2>
      <RouterLink to="/">← {{ t('common.back') }}</RouterLink>
    </header>

    <p v-if="items.length === 0" class="hint">{{ t('history.empty') }}</p>

    <ul v-else data-test="list" class="history-list">
      <li
        v-for="item in items"
        :key="`${JSON.stringify(item.sourceDescriptor)}::${item.relPath}`"
        data-test="row"
        class="history-row"
      >
        <span class="icon">📁</span>
        <button class="name" @click="openEntry(item)">{{ item.displayName }}</button>
        <span class="time">{{ formatDate(item.lastVisitedAt, 'system') }}</span>
        <button
          class="delete"
          :aria-label="t('common.delete')"
          @click="removeEntry(item)"
        >×</button>
      </li>
    </ul>
  </main>
</template>

<style scoped>
.history-view { padding: 24px; height: 100%; overflow-y: auto; }
.history-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
}
h2 { margin: 0; }
.history-list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 8px; }
.history-row {
  display: flex;
  gap: 16px;
  align-items: center;
  padding: 12px;
  border: 1px solid var(--color-border, #444);
  border-radius: 8px;
}
.icon { font-size: 18px; }
.name {
  flex: 1;
  background: none;
  border: none;
  color: inherit;
  text-align: left;
  cursor: pointer;
  font: inherit;
  padding: 0;
}
.name:hover { text-decoration: underline; }
.time { opacity: 0.7; font-size: 12px; }
.delete {
  background: none;
  border: 1px solid var(--color-border, #444);
  color: var(--color-text-muted, #888);
  width: 24px;
  height: 24px;
  border-radius: 4px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
}
.delete:hover { color: var(--color-error, #f87171); border-color: var(--color-error, #f87171); }
.hint { color: var(--color-text-muted, #888); text-align: center; margin-top: 24px; }
</style>
```

- [ ] **步骤 2：npm run type-check**

```bash
npm run type-check
```

预期：无错误。

- [ ] **步骤 3：Commit**

```bash
git add src/views/History.vue
git commit -m "feat(history): History.vue 重写 (folder 行 + 删除 + 跳 FileBrowser)"
```

---

## 任务 14：Frontend Library.vue — docstring "书架" → "书库"

**文件：**
- 修改：`src/views/Library.vue`

- [ ] **步骤 1：定位 docstring**

定位 `/** LibraryView.vue — 书架视图 ... */`，替换为：

```ts
/**
 * LibraryView.vue — 书库视图 (v0.1.0-module3.0)
 * - 仅显示 is_favorite=1 的手动加入书（Android LibraryEntity 对齐）
 * - 按 lastReadAt DESC 排
 * - 双击 ★ 切换 favorite 状态
 */
```

- [ ] **步骤 2：Commit**

```bash
git add src/views/Library.vue
git commit -m "docs(library): docstring 书架 → 书库"
```

---

## 任务 15：Frontend i18n — zh-CN.ts 术语替换

**文件：**
- 修改：`src/locales/zh-CN.ts`

- [ ] **步骤 1：grep 当前 zh-CN.ts 用 "书架" 的位置**

```bash
grep -n "书架" src/locales/zh-CN.ts
```

预期：找出 1-3 处（library.title / library.empty 等）。

- [ ] **步骤 2：替换 "书架" → "书库"**

对每处匹配，**仅修改 display string**，i18n key 不变。例如：

```ts
    // 旧
    library: {
      title: '书架',
      empty: '书架为空...',
    }
    // 新
    library: {
      title: '书库',
      empty: '书库为空...',
    }
```

- [ ] **步骤 3：npm run type-check**

```bash
npm run type-check
```

预期：无错误（i18n key 未变，仅 string value 改）。

- [ ] **步骤 4：Commit**

```bash
git add src/locales/zh-CN.ts
git commit -m "docs(i18n): zh-CN.ts 书架 → 书库（display string）"
```

---

## 任务 16：CLAUDE.md — 加 v0.1.0-module3.0 决策记录

**文件：**
- 修改：`CLAUDE.md`

- [ ] **步骤 1：定位 §6 决策记录末尾**

找到最近的决策条目（如 `numbering 范围` / `Rust 端不调 IPC 拿 metadata` / `不做百分比进度`）。

- [ ] **步骤 2：追加 4 条新决策**

```markdown
- **library 只显示 is_favorite=1** — 用户反馈明确；Android LibraryScreen 模式扩展（Android UI 仍列全部，桌面端过滤更严；temp row 用于 progress 持久化但不显示）
- **history 重写为 folder-level** — Android BrowseHistory 直接对齐；旧 per-book 行丢弃
- **`book` 表重命名为 `library`** — 与 Android LibraryEntity 字节级镜像，简化 backup/restore 对接
- **per-folder 排序覆盖** — Android DirectorySortEntity 对齐；locationKey = sourceDescriptor + "|" + relPath
```

- [ ] **步骤 3：定位 "当前状态" 表格**

找到 Phase 4 的行（"书签/喜欢/历史/书架/标签/搜索"），追加一行 Phase 4.5：

```markdown
| 4.5 | 书库 / 阅览记录 / directory_sort（Android schema 对齐） | 🟡 进行中；migration 005 + 9 个 Rust commands + 4 个 store 重写 |
```

或按现有表格风格调整行文字。

- [ ] **步骤 4：Commit**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md §6 加 v0.1.0-module3.0 决策记录 + 当前状态行"
```

---

## 任务 17：测试 — 既有 store 测试 mock 形状更新

**文件：**
- 修改：`src/stores/fileBrowser.test.ts`
- 修改：`src/stores/history.test.ts`
- 修改：`src/stores/library.test.ts`
- 修改：`src/composables/useReaderActions.test.ts`

- [ ] **步骤 1：history.test.ts — mock `listHistory` 返 `BrowseHistoryEntry[]`**

定位 `vi.mock('@/lib/tauri', ...)` 块里 `listHistory: vi.fn()`，mock 实现改为：

```ts
listHistory: vi.fn(async () => []),
recordHistory: vi.fn(async () => {}),
deleteHistory: vi.fn(async () => {}),
```

并把测试里 `HistoryItem` 改成 `BrowseHistoryEntry`。

- [ ] **步骤 2：fileBrowser.test.ts — mock `recordHistory` 验证 fetch 自动调**

在 `vi.mock('@/lib/tauri', ...)` 块加：

```ts
recordHistory: vi.fn(async () => {}),
getDirectorySort: vi.fn(async () => null),
setDirectorySort: vi.fn(async () => {}),
```

并新增测试：`fetch` 成功后 `recordHistory` 被调一次。

- [ ] **步骤 3：library.test.ts — `listLibrary` mock 返 `BookItem` 11 列**

更新 mock 形状（BookItem 现在有 11 字段）。

- [ ] **步骤 4：useReaderActions.test.ts — 重写：favorite 参数**

定位现有 `readNow` / `addToLibrary` 测试，改 mock：

```ts
vi.mock('@/lib/tauri', async () => {
  const actual = await vi.importActual<typeof import('@/lib/tauri')>('@/lib/tauri');
  return {
    ...actual,
    createBook: vi.fn(async () => 42),
    listDirectory: vi.fn(async () => []),
    listHistory: vi.fn(async () => []),  // 删
    recordHistory: vi.fn(async () => {}),  // 删
  };
});
```

加测试：
- `readNow` 调 `createBook({ favorite: false, ... })`
- `addToLibrary` 调 `createBook({ favorite: true, ... })`

- [ ] **步骤 5：npm run type-check + npm test**

```bash
npm run type-check && npm test -- --run
```

预期：type-check 通过；既有测试 0 fail（mock 形状已更新）。

- [ ] **步骤 6：Commit**

```bash
git add src/stores/fileBrowser.test.ts src/stores/history.test.ts src/stores/library.test.ts src/composables/useReaderActions.test.ts
git commit -m "test: 更新 store mock 形状 (BrowseHistoryEntry / CreateBookArgs / directorySort)"
```

---

## 任务 18：测试 — 新增 directorySort.test.ts + History.test.ts

**文件：**
- 新建：`src/stores/directorySort.test.ts`
- 新建：`src/views/History.test.ts`

- [ ] **步骤 1：directorySort.test.ts 4 用例**

新建文件：

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { setActivePinia, createPinia } from 'pinia';
import { useDirectorySortStore } from '@/stores/directorySort';
import { getDirectorySort, setDirectorySort, type SourceDescriptor } from '@/lib/tauri';

vi.mock('@/lib/tauri', async () => {
  const actual = await vi.importActual<typeof import('@/lib/tauri')>('@/lib/tauri');
  return {
    ...actual,
    getDirectorySort: vi.fn(),
    setDirectorySort: vi.fn(async () => {}),
  };
});

const sd: SourceDescriptor = { type: 'local', rootPath: 'C:/comics' };

beforeEach(() => {
  setActivePinia(createPinia());
  vi.clearAllMocks();
});

describe('useDirectorySortStore', () => {
  it('resolve 未命中 → 返回 null', async () => {
    vi.mocked(getDirectorySort).mockResolvedValueOnce(null);
    const store = useDirectorySortStore();
    expect(await store.resolve(sd, 'Vol.01')).toBeNull();
  });

  it('resolve 命中 → 缓存 + 返回', async () => {
    vi.mocked(getDirectorySort).mockResolvedValueOnce({
      locationKey: 'x', sortField: 'name', ascending: false,
    });
    const store = useDirectorySortStore();
    const first = await store.resolve(sd, 'Vol.01');
    expect(first).toEqual({ sortField: 'name', ascending: false });
    // 第二次命中缓存
    expect(await store.resolve(sd, 'Vol.01')).toEqual({ sortField: 'name', ascending: false });
    expect(getDirectorySort).toHaveBeenCalledTimes(1);
  });

  it('set → 写 cache + 调 setDirectorySort', async () => {
    const store = useDirectorySortStore();
    await store.set(sd, 'Vol.01', { sortField: 'date', ascending: true });
    expect(setDirectorySort).toHaveBeenCalledWith(sd, 'Vol.01', 'date', true);
    // 后续 resolve 命中缓存
    expect(await store.resolve(sd, 'Vol.01')).toEqual({ sortField: 'date', ascending: true });
  });

  it('resolve IPC 失败 → 返回 null（不抛）', async () => {
    vi.mocked(getDirectorySort).mockRejectedValueOnce(new Error('boom'));
    const store = useDirectorySortStore();
    expect(await store.resolve(sd, 'Vol.02')).toBeNull();
  });
});
```

- [ ] **步骤 2：History.test.ts 3 用例**

新建文件：

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { mount, flushPromises } from '@vue/test-utils';
import { createPinia, setActivePinia } from 'pinia';
import { createRouter, createMemoryHistory } from 'vue-router';
import History from '@/views/History.vue';
import { useHistoryStore } from '@/stores/history';
import { useFileBrowserStore } from '@/stores/fileBrowser';
import type { BrowseHistoryEntry } from '@/lib/tauri';

vi.mock('@/lib/tauri', async () => {
  const actual = await vi.importActual<typeof import('@/lib/tauri')>('@/lib/tauri');
  return {
    ...actual,
    listHistory: vi.fn(),
    recordHistory: vi.fn(async () => {}),
    deleteHistory: vi.fn(async () => {}),
  };
});

const router = createRouter({
  history: createMemoryHistory(),
  routes: [
    { path: '/', name: 'home', component: { template: '<div/>' } },
    { path: '/history', component: History },
  ],
});

beforeEach(() => {
  setActivePinia(createPinia());
  vi.clearAllMocks();
});

const sample: BrowseHistoryEntry[] = [
  {
    sourceDescriptor: { type: 'local', rootPath: 'C:/comics' },
    relPath: '',
    displayName: 'root',
    lastVisitedAt: 1000,
  },
  {
    sourceDescriptor: { type: 'local', rootPath: 'C:/comics' },
    relPath: 'Vol.01',
    displayName: 'Vol.01',
    lastVisitedAt: 2000,
  },
];

describe('History.vue', () => {
  it('mount → 列 items', async () => {
    const { listHistory } = await import('@/lib/tauri');
    vi.mocked(listHistory).mockResolvedValueOnce(sample);
    const wrapper = mount(History, { global: { plugins: [router] } });
    await flushPromises();
    const rows = wrapper.findAll('[data-test="row"]');
    expect(rows.length).toBe(2);
    expect(rows[0].text()).toContain('Vol.01');
    expect(rows[1].text()).toContain('root');
  });

  it('点击 row → setRoot + navigate + router.push home', async () => {
    const { listHistory } = await import('@/lib/tauri');
    vi.mocked(listHistory).mockResolvedValueOnce(sample);
    const wrapper = mount(History, { global: { plugins: [router] } });
    await flushPromises();
    const fb = useFileBrowserStore();
    const setRootSpy = vi.spyOn(fb, 'setRoot').mockResolvedValue();
    const navigateSpy = vi.spyOn(fb, 'navigate').mockResolvedValue();
    const pushSpy = vi.spyOn(router, 'push');

    await wrapper.findAll('button.name')[0]!.trigger('click');
    expect(setRootSpy).toHaveBeenCalledWith('C:/comics');
    expect(navigateSpy).toHaveBeenCalledWith('Vol.01');
    expect(pushSpy).toHaveBeenCalledWith({ name: 'home' });
  });

  it('删除按钮 → deleteEntry + refresh', async () => {
    const { listHistory, deleteHistory } = await import('@/lib/tauri');
    vi.mocked(listHistory).mockResolvedValueOnce(sample);
    const wrapper = mount(History, { global: { plugins: [router] } });
    await flushPromises();
    const store = useHistoryStore();
    const refreshSpy = vi.spyOn(store, 'refresh');

    await wrapper.findAll('button.delete')[0]!.trigger('click');
    await flushPromises();
    expect(deleteHistory).toHaveBeenCalledWith(
      sample[0]!.sourceDescriptor,
      sample[0]!.relPath,
    );
    expect(refreshSpy).toHaveBeenCalled();
  });
});
```

- [ ] **步骤 3：npm test**

```bash
npm test -- --run
```

预期：新测试 7 个 case 全部通过；既有测试 0 fail。

- [ ] **步骤 4：Commit**

```bash
git add src/stores/directorySort.test.ts src/views/History.test.ts
git commit -m "test: 新增 directorySort.test (4) + History.test (3)"
```

---

## 任务 19：测试 — Rust commands 单元测试（可选加分项）

**文件：**
- 修改：`src-tauri/src/commands/library.rs`（测试块）
- 修改：`src-tauri/src/commands/history.rs`（测试块）
- 新建：`src-tauri/src/commands/directory_sort.rs` 测试块

- [ ] **步骤 1：library.rs 底部加 `#[cfg(test)]`**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::db::Db;
    use rusqlite::Connection;

    fn test_db() -> Db {
        let conn = Connection::open_in_memory().unwrap();
        Db::new_for_test(conn)  // 视乎现有 Db 是否有 test constructor
    }

    #[test]
    fn create_book_inserts_new_row() {
        // ...
    }
}
```

**注意**：现有 `Db` 类型是否暴露了 `new_for_test` 或类似 test-only constructor，**先检查** `src-tauri/src/db/mod.rs` 或 `lib.rs`。如果没有，需要先加。

**如果 Db 不暴露 test constructor，本任务跳过**（标记为后续优化），跳过不影响主流程。

- [ ] **步骤 2：cargo test**

```bash
cd src-tauri && cargo test -p mirapage-desktop-lib
```

预期：测试通过；如果跳过本任务也 OK。

- [ ] **步骤 3：Commit（如有测试代码）**

```bash
git add src-tauri/src/commands/library.rs src-tauri/src/commands/history.rs src-tauri/src/commands/directory_sort.rs
git commit -m "test(rust): library / history / directory_sort 单元测试（migration 005 行为）"
```

如果跳过：`git commit --allow-empty -m "skip: rust 单元测试（Db test constructor 待加，后续 PR）"`

---

## 任务 20：最终验收 — type-check + 全测 + 手动冒烟

**文件：**
- 无（验证步骤）

- [ ] **步骤 1：cargo check + cargo test**

```bash
cd src-tauri && cargo check && cargo test -p mirapage-desktop-lib
```

预期：编译通过；所有 cargo test 通过。

- [ ] **步骤 2：npm type-check + npm test**

```bash
npm run type-check && npm test -- --run
```

预期：type-check 0 error；所有 vitest 通过（既有 + 7 新 = ~310 用例）。

- [ ] **步骤 3：手动冒烟（按规格 §7）**

打开 dev 模式（`npm run tauri:dev`）：

```text
1. FileBrowser 双击目录 → "立即阅读" → Library 不出现该书（is_favorite=0 过滤）
2. FileBrowser 选中目录 → "加入书库" → Library 出现该书（is_favorite=1）
3. FileBrowser 上下导航 3 个目录 → History 出现 3 条文件夹（按时间倒序）
4. History 点击一行 → 跳回 FileBrowser 对应 root + path
5. History 删除一行 → 行消失
6. 同一文件夹再访问 → last_visited_at refresh（不新增行）
7. FileBrowser 改排序（按大小）→ 离开再回来 → 仍按大小（directory_sort override 起作用）
```

- [ ] **步骤 4：打 tag + push**

```bash
git tag v0.1.0-module3.0
git push github main
git push github v0.1.0-module3.0
```

预期：tag 已发布；远程 ahead = 0。

- [ ] **步骤 5：Commit（验收记录）**

```bash
git commit --allow-empty -m "chore: v0.1.0-module3.0 验证完成 — type-check + 全测 + 手动冒烟通过"
```

---

## 自检

**1. 规格覆盖度：**

| 规格节 | 实现任务 |
|---|---|
| §1.1 用户反馈 3 条（书架→书库 / 手动添加 / history 不可用） | 任务 15（术语）+ 任务 2/3（library filter）+ 任务 4/9/13（history 重写） |
| §1.2 目标 3 条（library 严格手动 / history folder-level / schema 对齐） | 任务 2/3 + 任务 4/9/13 + 任务 1 |
| §1.3 非目标 | 全程遵守（不删 library row / 不做 backup 互通 / 不渲染封面图 / 不做 recently read 视图） |
| §3.1-3.5 schema | 任务 1 |
| §4.1 library.rs 重写 | 任务 2 + 任务 3 |
| §4.2 history.rs 重写 | 任务 4 |
| §4.3 progress.rs 清理 | 任务 6 |
| §4.4 directory_sort.rs 新建 | 任务 5 |
| §4.5 lib.rs 注册 | 任务 7 |
| §5.1 tauri.ts | 任务 8 |
| §5.2 useReaderActions | 任务 12 |
| §5.3 fileBrowser 自动记录 | 任务 11 |
| §5.4 history store | 任务 9 |
| §5.5 History.vue 重写 | 任务 13 |
| §5.6 Library.vue docstring | 任务 14 |
| §5.7 i18n 术语 | 任务 15 |
| §5.9 directorySort store | 任务 10 |
| §5.10 fileBrowser 集成 directorySort | 任务 11 |
| §6.1 测试覆盖 | 任务 17/18/19 |
| §7 验收 | 任务 20 |

**遗漏**：无。

**2. 占位符扫描：** grep "TODO\|待定\|FIXME\|后续实现" — 0 命中。

**3. 类型一致性：**
- `CreateBookArgs` 任务 3（Rust）↔ 任务 8（TS）↔ 任务 12（useReaderActions）一致
- `BrowseHistoryEntry` 任务 4（Rust）↔ 任务 8（TS）↔ 任务 9（store）一致
- `DirectorySort` 任务 5（Rust）↔ 任务 8（TS）↔ 任务 10（store）一致
- `BookItem` 任务 2（Rust）↔ 任务 17（library.test.ts）一致
- `setSortField` 任务 11 写入 → 任务 11 `sortedEntries` 读 `effectiveSortField` 一致

**OK，开始执行。**