# module3.0.14 小件打包实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 落地 spec `docs/superpowers/specs/2026-08-16-module3.0.14-small-fixes-design.md` 的 9 项小件（A CI 测试 / B webdav 修复 / C 跨卷竞态 / D 重置进度 / E 喜欢 toggle / F ease-out / G 文档 / H 菜单二合一 / I 选中描边环）。

**架构：** 7 项独立小修 + 2 个新 Rust 命令（`reset_progress_by_location` / `get_book_status`，共用 `find_library_id_by_location` helper）+ FileBrowser 统一持有喜欢状态表（`descriptorId + '|' + absPath` 为键，三入口下传）。

**技术栈：** Tauri 2.x（Rust + quick-xml + rusqlite）/ Vue 3 + Pinia + Vitest（happy-dom）/ GitHub Actions。

**实施偏差说明**（相对 spec，已核实代码后修正）：
1. `get_book_status` 放 `commands/library.rs`（spec 初稿误写 progress.rs，**spec 已同步修正**）——与 `set_favorite`/`list_library` 同域，helper 也在 library.rs。
2. FileBrowser 的 `onLibraryChanged` 实际是 `readStatus.refresh()`（非 library.refresh）——toggle 后只更新本地状态表即可，不调它（favorite 不影响 readStatus）。
3. `useCrossVolume.test.ts:684` 既有用例断言 identity-null **不**消费 flag（旧行为）——C 项 TDD 的第一步就是反转该断言（红），再改源码（绿）。

**CRLF 警告**：`DESIGN.md` / `AGENTS.md` 是 CRLF 文件——多行 Edit 必失配，用单行锚点 Edit 或一次性 node 补丁脚本（见任务 11）。

---

## 文件结构

| 文件 | 变更 | 职责 |
|---|---|---|
| `src-tauri/src/source/webdav_impl.rs` | 修改 | B：local_name + Empty/Start collection 解析 + 新测试 |
| `.github/workflows/verify.yml` | 修改 | A：补 cargo test 步骤 |
| `src/composables/useCrossVolume.ts` + `.test.ts` | 修改 | C：早退消费 flag |
| `src-tauri/src/commands/library.rs` | 修改 | D/E：`find_library_id_by_location` helper + `get_book_status` 命令 + 测试 |
| `src-tauri/src/commands/progress.rs` | 修改 | D：`reset_progress_by_location` 命令 + 测试 |
| `src-tauri/src/lib.rs` | 修改 | D/E：generate_handler 注册 2 命令 |
| `src/lib/tauri.ts` | 修改 | D/E：`resetProgressByLocation` / `getBookStatus` |
| `src/components/filebrowser/RowContextMenu.vue` + `.test.ts` | 修改 | D：重置重写；E：喜欢二态；H：删 retry 项 |
| `src/components/filebrowser/FileBrowser.vue` + `.test.ts` | 修改 | E：状态表 + toggle + 工具栏；H：删 retry 转发 |
| `src/components/filebrowser/EntryDetailPanel.vue` | 修改 | E：喜欢二态 emit |
| `src/components/filebrowser/FileList.vue` | 修改 | H：删 retryBatch |
| `src/components/filebrowser/MasonryView.vue` + `.test.ts` | 修改 | H：删 retryBatch expose + mock 行 |
| `src/composables/useMasonryThumbnails.ts` | 修改 | H：删 retryBatch |
| `src/components/filebrowser/MasonryRow.vue` + `.test.ts` | 修改 | I：::after 描边环 + 源码守卫 |
| `src/components/filebrowser/FileIcon.vue` | 修改 | F：ease-out |
| `src/locales/zh-CN.ts` / `en-US.ts` | 修改 | E：+reader.liked；H：−retryThumbnail×2 |
| `DESIGN.md` / `AGENTS.md` | 修改 | G：§16 与状态表同步 |

---

### 任务 1：webdav parse_propfind 修复（spec B）

**文件：**
- 修改：`src-tauri/src/source/webdav_impl.rs`（parse_propfind 48-94 行区 + tests mod）

- [ ] **步骤 1：写失败测试（无前缀 + 展开式 collection）**

在 `mod tests` 末尾追加：

```rust
#[test]
fn parse_propfind_handles_unprefixed_and_expanded_collection() {
    // module3.0.14 spec B：无命名空间前缀 + 展开式 <collection></collection>
    // （与既有前缀 + 自闭合用例分别覆盖两条 collection 解析路径）
    let body = r#"<?xml version="1.0"?>
<multistatus xmlns="DAV:">
  <response>
    <href>/dav/sub1/</href>
    <propstat><prop>
      <resourcetype><collection></collection></resourcetype>
    </prop></propstat>
  </response>
  <response>
    <href>/dav/pic.zip</href>
    <propstat><prop>
      <getcontentlength>999</getcontentlength>
    </prop></propstat>
  </response>
</multistatus>"#;
    let entries = WebDavMediaSource::parse_propfind(body, "/dav/").unwrap();
    assert_eq!(entries.len(), 2);
    assert_eq!(entries[0].name, "pic.zip");
    assert_eq!(entries[0].size, 999);
    assert!(!entries[0].is_directory);
    assert_eq!(entries[1].name, "sub1");
    assert!(entries[1].is_directory);
}
```

- [ ] **步骤 2：运行验证失败（2 个用例红）**

```bash
cd src-tauri && cargo test parse_propfind
```

预期：`parse_propfind_extracts_collection_and_files` FAILED（left: 0, right: 2）+ 新用例 FAILED（同样 0 条）。

- [ ] **步骤 3：实现修复**

(a) 在 `parse_propfind` 上方加辅助函数：

```rust
/// 取标签 local name（剥命名空间前缀，`d:response` → `response`；无前缀原样返回）。
fn local_name(name: &[u8]) -> &str {
    let s = std::str::from_utf8(name).unwrap_or("");
    match s.split_once(':') {
        Some((_, local)) => local,
        None => s,
    }
}
```

(b) `Event::Start` 分支改为（local name 匹配 + 展开式 collection）：

```rust
Ok(Event::Start(e)) => {
    let name = local_name(e.name().as_ref()).to_owned();
    match name.as_str() {
        "response" => current = Some(PartialEntry::default()),
        "href" | "getcontentlength" | "resourcetype" | "getlastmodified" => {
            current_field = Some(name);
        }
        // 展开式 <collection></collection>：Start 事件到达
        "collection" => {
            if current_field.as_deref() == Some("resourcetype") {
                if let Some(cur) = current.as_mut() {
                    cur.is_collection = true;
                }
            }
        }
        _ => {}
    }
}
```

(c) `Event::End` 分支的 `let name = ...` 行改为 local name：

```rust
let name = local_name(e.name().as_ref());
```

（其余 response 收尾逻辑不动。）

(d) 在 `Event::Text` 分支后新增 `Event::Empty` 分支：

```rust
Ok(Event::Empty(e)) => {
    // 自闭合 <d:collection/>：quick-xml 以 Empty 事件到达（不拆成 Start+End）
    if local_name(e.name().as_ref()) == "collection"
        && current_field.as_deref() == Some("resourcetype")
    {
        if let Some(cur) = current.as_mut() {
            cur.is_collection = true;
        }
    }
}
```

- [ ] **步骤 4：运行验证通过**

```bash
cd src-tauri && cargo test parse_propfind
```

预期：2 passed（含原本红的既有用例转绿）。

- [ ] **步骤 5：全量 Rust 测试确认无回归**

```bash
cd src-tauri && cargo test
```

预期：0 failed（main 基线唯一红即本用例）。

- [ ] **步骤 6：Commit**

```bash
git add src-tauri/src/source/webdav_impl.rs
git commit -m "fix(webdav): parse_propfind 支持命名空间前缀与自闭合/展开式 collection

- local_name() 剥 d: 前缀，Start/End 匹配改用 local name（无前缀 XML 天然兼容）
- 新增 Event::Empty 分支识别 <collection/>，Start 分支识别展开式 <collection></collection>
- 新增无前缀+展开式用例，与前缀+自闭合用例分别覆盖两条解析路径"
```

---

### 任务 2：CI 补 cargo test（spec A，必须在任务 1 之后）

**文件：**
- 修改：`.github/workflows/verify.yml`（Cargo check 步骤后）

- [ ] **步骤 1：加步骤**

在 `- name: Cargo check` 块之后追加：

```yaml
      - name: Cargo test
        run: cargo test --manifest-path src-tauri/Cargo.toml
```

- [ ] **步骤 2：本地等价验证**

```bash
cd src-tauri && cargo test
```

预期：0 failed（CI 将跑同一命令；本地绿 = CI 绿）。

- [ ] **步骤 3：Commit**

```bash
git add .github/workflows/verify.yml
git commit -m "ci: verify 工作流补 cargo test 步骤

Rust 回归不再静默（webdav 用例红数月无人知的直接对策）"
```

---

### 任务 3：useCrossVolume 早退消费 flag（spec C）

**文件：**
- 修改：`src/composables/useCrossVolume.ts:183-184`
- 修改：`src/composables/useCrossVolume.test.ts`（~684 行既有用例）

- [ ] **步骤 1：反转既有断言（红）**

把 `it('identity() === null → maybeContinue 直接 return', ...)` 用例改为：

```ts
  it('identity() === null → maybeContinue 直接 return 且消费 pendingNextVolume', async () => {
    const { cv, navigateToVolume, pushToast, consumePendingNextVolume } = setup({ currentIdentity: null, continueMode: 'auto' });

    await cv.maybeContinue(true, 'next');

    expect(vi.mocked(findNextVolume)).not.toHaveBeenCalled();
    expect(navigateToVolume).not.toHaveBeenCalled();
    expect(pushToast).not.toHaveBeenCalled();
    // module3.0.14 spec C：对齐 !canStart() 分支——早退前消费，防 flag 卡 true
    expect(consumePendingNextVolume).toHaveBeenCalled();
    expect(cv.phase.value).toBe('idle');
  });
```

- [ ] **步骤 2：运行验证失败**

```bash
npx vitest run src/composables/useCrossVolume.test.ts
```

预期：该用例 FAIL（`consumePendingNextVolume` 未被调用）。

- [ ] **步骤 3：实现**

`useCrossVolume.ts` 183-184 行：

```ts
    const startIdentity = opts.identity();
    if (!startIdentity) {
      // module3.0.14 spec C：对齐 !canStart() 分支——早退前消费 flag，防末页重试失效
      opts.consumePendingNextVolume();
      return;
    }
```

- [ ] **步骤 4：运行验证通过**

```bash
npx vitest run src/composables/useCrossVolume.test.ts
```

预期：全部 PASS。

- [ ] **步骤 5：Commit**

```bash
git add src/composables/useCrossVolume.ts src/composables/useCrossVolume.test.ts
git commit -m "fix(reader): useCrossVolume identity-null 早退消费 pendingNextVolume

窄竞态：换书瞬间拿不到卷身份时直接退出，flag 不清导致后续末页重试
不再触发 watch（Alt+→ force 路径不受影响）。对齐 canStart 分支模式"
```

---

### 任务 4：Rust 端 D+E 基础——helper + reset_progress_by_location + get_book_status

**文件：**
- 修改：`src-tauri/src/commands/library.rs`（helper + get_book_status + 测试）
- 修改：`src-tauri/src/commands/progress.rs`（reset 命令 + 测试）
- 修改：`src-tauri/src/lib.rs`（generate_handler 注册）

- [ ] **步骤 1：写失败测试（library.rs tests mod 追加）**

先在 tests mod 加公共辅助（若已有等价辅助则复用）。**fixture 用真实序列化构造**（审查 P2：手写 JSON 里 `R:\comics` 反斜杠未转义是非法 JSON，「同错比对同错」会假绿，且无法验证 `serde_json::to_string(SourceDescriptor)` 真实输出）：

```rust
    fn local_descriptor_json() -> String {
        serde_json::to_string(&crate::source::descriptor::SourceDescriptor::Local {
            root_path: r"R:\comics".to_string(),
        })
        .unwrap()
    }

    fn insert_library_row(conn: &rusqlite::Connection, abs_path: &str, favorite: bool) -> i64 {
        conn.execute(
            "INSERT INTO library (title, source_descriptor, source_type, absolute_path, added_at, is_favorite)
             VALUES ('t', ?1, 'local', ?2, 0, ?3)",
            rusqlite::params![local_descriptor_json(), abs_path, favorite as i64],
        )
        .unwrap();
        conn.last_insert_rowid()
    }

    #[test]
    fn find_library_id_by_location_matches_descriptor_and_normalized_path() {
        let db = crate::db::Db::open_in_memory().unwrap();
        let conn = db.conn();
        let id = insert_library_row(&conn, "comics/vol1", false);
        let descriptor = local_descriptor_json();

        assert_eq!(
            super::find_library_id_by_location(&conn, &descriptor, "comics/vol1"),
            Some(id)
        );
        assert_eq!(
            super::find_library_id_by_location(&conn, &descriptor, "other/vol9"),
            None
        );
        // 不同 descriptor（rootPath 不同）同相对路径 → 不串
        let other = serde_json::to_string(
            &crate::source::descriptor::SourceDescriptor::Local { root_path: r"D:\x".to_string() },
        )
        .unwrap();
        assert_eq!(
            super::find_library_id_by_location(&conn, &other, "comics/vol1"),
            None
        );
    }

    #[test]
    fn get_book_status_returns_favorite_and_temp_rows() {
        let db = crate::db::Db::open_in_memory().unwrap();
        let conn = db.conn();
        insert_library_row(&conn, "comics/vol1", true);
        insert_library_row(&conn, "comics/vol2", false);
        let descriptor = local_descriptor_json();

        // favorite 行
        let st = super::get_book_status_inner(&conn, &descriptor, "comics/vol1").unwrap();
        assert!(st.as_ref().unwrap().is_favorite);
        // temp 行（读过未喜欢）——list_library 查不到，这里必须查得到
        let st = super::get_book_status_inner(&conn, &descriptor, "comics/vol2").unwrap();
        assert!(!st.as_ref().unwrap().is_favorite);
        // 无行
        let st = super::get_book_status_inner(&conn, &descriptor, "nope").unwrap();
        assert!(st.is_none());
    }
```

- [ ] **步骤 2：写失败测试（progress.rs tests mod 追加）**

```rust
    #[test]
    fn reset_progress_by_location_clears_finished_page_and_image_name() {
        let db = setup_db();
        let conn = db.conn();
        let book_id = insert_library_row_for_reset(&conn, "comics/vol1");
        conn.execute(
            "INSERT INTO progress (book_id, page, reader_mode, updated_at, finished, image_name)
             VALUES (?1, 42, 'single', 1, 1, 'page-020.jpg')",
            rusqlite::params![book_id],
        )
        .unwrap();

        let descriptor = local_descriptor_json();
        let ok = super::reset_progress_by_location_inner(&conn, &descriptor, "comics/vol1").unwrap();
        assert!(ok);

        let (page, finished, image): (i64, i64, Option<String>) = conn
            .query_row(
                "SELECT page, finished, image_name FROM progress WHERE book_id = ?1",
                rusqlite::params![book_id],
                |r| Ok((r.get(0)?, r.get(1)?, r.get(2)?)),
            )
            .unwrap();
        assert_eq!((page, finished, image), (0, 0, None));
    }

    #[test]
    fn reset_progress_by_location_no_library_row_or_no_progress_row() {
        let db = setup_db();
        let conn = db.conn();
        let descriptor = local_descriptor_json();
        // library 无行 → false
        assert!(!super::reset_progress_by_location_inner(&conn, &descriptor, "nope").unwrap());
        // library 有行但无 progress 行 → false
        let book_id = insert_library_row_for_reset(&conn, "comics/vol2");
        let _ = book_id;
        assert!(!super::reset_progress_by_location_inner(&conn, &descriptor, "comics/vol2").unwrap());
    }
```

progress.rs tests mod 同时加两个辅助（descriptor 构造与 library.rs 同式——tests mod 相互独立，小体积重复可接受）：

```rust
    fn local_descriptor_json() -> String {
        serde_json::to_string(&crate::source::descriptor::SourceDescriptor::Local {
            root_path: r"R:\comics".to_string(),
        })
        .unwrap()
    }

    fn insert_library_row_for_reset(conn: &rusqlite::Connection, abs_path: &str) -> i64 {
        conn.execute(
            "INSERT INTO library (title, source_descriptor, source_type, absolute_path, added_at, is_favorite)
             VALUES ('t', ?1, 'local', ?2, 0, 0)",
            rusqlite::params![local_descriptor_json(), abs_path],
        )
        .unwrap();
        conn.last_insert_rowid()
    }
```

- [ ] **步骤 3：运行验证失败**

```bash
cd src-tauri && cargo test
```

（cargo test 只接受**单个**过滤词，多过滤词命令无效——审查 P2 已修正。新测试引用未定义函数时整个测试目标编译失败，即为红。）

预期：编译错误（`find_library_id_by_location` / `get_book_status_inner` / `reset_progress_by_location_inner` 不存在）。

- [ ] **步骤 4：实现**

(a) `library.rs`：在 `set_favorite` 附近加：

```rust
/// 单本书的喜欢状态（module3.0.14，serde camelCase 与 TS BookStatus 镜像）。
#[derive(Debug, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct BookStatus {
    pub book_id: i64,
    pub is_favorite: bool,
}

/// 按位置精确查 library 行 id（module3.0.14）。
/// abs_path 经 validate_source_relative 归一（与 find_next_volume::sibling_is_finished 同语义）。
pub(crate) fn find_library_id_by_location(
    conn: &rusqlite::Connection,
    descriptor_str: &str,
    abs_path: &str,
) -> Option<i64> {
    let norm = crate::algorithm::validate_source_relative(abs_path)
        .unwrap_or_else(|_| abs_path.to_string());
    conn.query_row(
        "SELECT id FROM library WHERE source_descriptor = ?1 AND absolute_path = ?2",
        rusqlite::params![descriptor_str, norm],
        |r| r.get(0),
    )
    .ok()
}

pub(crate) fn get_book_status_inner(
    conn: &rusqlite::Connection,
    descriptor_str: &str,
    abs_path: &str,
) -> rusqlite::Result<Option<BookStatus>> {
    let Some(book_id) = find_library_id_by_location(conn, descriptor_str, abs_path) else {
        return Ok(None);
    };
    let fav = conn.query_row(
        "SELECT is_favorite FROM library WHERE id = ?1",
        rusqlite::params![book_id],
        |r| r.get::<_, i64>(0),
    )?;
    Ok(Some(BookStatus { book_id, is_favorite: fav != 0 }))
}

/// 按位置精确查单本书喜欢状态（不筛 favorite、无分页）。
/// 文件浏览器喜欢按钮二态用；list_library 只回 favorite 行且有分页截断，前端匹配不可靠。
#[tauri::command]
pub fn get_book_status(
    descriptor: crate::source::descriptor::SourceDescriptor,
    abs_path: String,
    db: tauri::State<crate::db::Db>,
) -> Result<Option<BookStatus>, String> {
    let descriptor_str = serde_json::to_string(&descriptor).map_err(|e| e.to_string())?;
    get_book_status_inner(&*db.conn(), &descriptor_str, &abs_path).map_err(|e| e.to_string())
}
```

（`Serialize` 若未 import 则补 `use serde::Serialize;`。）

(b) `progress.rs`：在 `mark_finished` 后加：

```rust
fn reset_progress_by_location_inner(
    conn: &rusqlite::Connection,
    descriptor_str: &str,
    abs_path: &str,
) -> rusqlite::Result<bool> {
    let Some(book_id) =
        crate::commands::library::find_library_id_by_location(conn, descriptor_str, abs_path)
    else {
        return Ok(false);
    };
    let n = conn.execute(
        "UPDATE progress SET page = 0, image_name = NULL, finished = 0, updated_at = ?2
         WHERE book_id = ?1",
        rusqlite::params![book_id, chrono_now()],
    )?;
    Ok(n > 0)
}

/// 重置指定位置的书阅读进度（module3.0.14）：finished=0 + page=0 + image_name=NULL。
/// library 无此书 / 无 progress 行 → Ok(false)，无副作用（不 upsert）。
#[tauri::command]
pub fn reset_progress_by_location(
    descriptor: crate::source::descriptor::SourceDescriptor,
    abs_path: String,
    db: tauri::State<crate::db::Db>,
) -> Result<bool, String> {
    let descriptor_str = serde_json::to_string(&descriptor).map_err(|e| e.to_string())?;
    reset_progress_by_location_inner(&*db.conn(), &descriptor_str, &abs_path).map_err(|e| e.to_string())
}
```

(c) `lib.rs` generate_handler：在 `commands::progress::mark_finished,` 行后追加两行：

```rust
            commands::progress::reset_progress_by_location,
            commands::library::get_book_status,
```

- [ ] **步骤 5：运行验证通过**

```bash
cd src-tauri && cargo test
```

预期：0 failed（新增 4 测试全绿）。

- [ ] **步骤 6：Commit**

```bash
git add src-tauri/src/commands/library.rs src-tauri/src/commands/progress.rs src-tauri/src/lib.rs
git commit -m "feat(commands): 按位置查书/重置进度——get_book_status + reset_progress_by_location

- find_library_id_by_location：descriptor JSON + validate_source_relative 归一精确匹配
- get_book_status：不筛 favorite 无分页，修 list_library 前端匹配两盲区
- reset_progress_by_location：清 finished+page+image_name（无行 no-op 不 upsert）"
```

---

### 任务 5：D 前端——RowContextMenu 重置重写

**文件：**
- 修改：`src/lib/tauri.ts`（markFinished 附近）
- 修改：`src/components/filebrowser/RowContextMenu.vue`（imports + onResetProgress 83-113 行）
- 修改：`src/components/filebrowser/RowContextMenu.test.ts`

- [ ] **步骤 1：写失败测试（RowContextMenu.test.ts）**

文件顶部（import RowContextMenu 之前）加 mock：

```ts
vi.mock('@/lib/tauri', () => ({
  resetProgressByLocation: vi.fn(async () => true),
}));
import { resetProgressByLocation } from '@/lib/tauri';
```

（`vi` 需加入既有 vitest import。）文件末尾追加 describe：

```ts
describe('RowContextMenu 重置阅读进度（module3.0.14）', () => {
  beforeEach(() => {
    setActivePinia(createPinia());
    vi.mocked(resetProgressByLocation).mockClear();
  });

  async function setupStore(base: string) {
    const fb = useFileBrowserStore();
    fb.rootPath = 'R:\\comics';
    fb.lastFetchedPath = base;
    return fb;
  }

  it('目录项：absPath = join(lastFetchedPath, entry.path)', async () => {
    await setupStore('');
    const w = await mountCtx(dirEntry('vol01'));
    await w.find('[data-test="reset-progress"]').trigger('click');
    await flushPromisesAndTick(w);
    expect(resetProgressByLocation).toHaveBeenCalledWith(
      { type: 'local', rootPath: 'R:\\comics' },
      'vol01',
    );
  });

  it('子目录内右键图片：absPath = lastFetchedPath 本身（书=所在目录）', async () => {
    await setupStore('output');
    const w = await mountCtx(imgEntry('page-001.jpg'));
    await w.find('[data-test="reset-progress"]').trigger('click');
    await flushPromisesAndTick(w);
    expect(resetProgressByLocation).toHaveBeenCalledWith(
      { type: 'local', rootPath: 'R:\\comics' },
      'output',
    );
  });

  it('返回 false 不抛错（no-op 场景）', async () => {
    await setupStore('');
    vi.mocked(resetProgressByLocation).mockResolvedValueOnce(false);
    const w = await mountCtx(dirEntry('vol01'));
    await w.find('[data-test="reset-progress"]').trigger('click');
    await flushPromisesAndTick(w);
    expect(w.emitted('close')).toBeTruthy();
  });
});

async function flushPromisesAndTick(w: { vm: { $nextTick: () => Promise<void> } }) {
  await new Promise((r) => setTimeout(r, 0));
  await w.vm.$nextTick();
}
```

（`useFileBrowserStore` 需 import；`flushPromises` 亦可用 @vue/test-utils 的。）

- [ ] **步骤 2：运行验证失败**

```bash
npx vitest run src/components/filebrowser/RowContextMenu.test.ts
```

预期：新用例 FAIL（调用的是旧 markFinished 流程 / 参数不符）。

- [ ] **步骤 3：实现**

(a) `tauri.ts` 在 `markFinished` 后加：

```ts
/** module3.0.14：按位置重置阅读进度（清 finished+page+image_name）。返回是否有行被重置。 */
export async function resetProgressByLocation(
  descriptor: SourceDescriptor,
  absPath: string,
): Promise<boolean> {
  return invoke<boolean>('reset_progress_by_location', { descriptor, absPath });
}
```

(b) `RowContextMenu.vue` imports：删 `useLibraryStore` 与 `markFinished`；加：

```ts
import { resetProgressByLocation } from '@/lib/tauri';
import { PathUtils } from '@/lib/path';
import type { SourceDescriptorLocal } from '@/lib/sourceDescriptor';
```

（`useFileBrowserStore` / `useReadStatusStore` / `log` 保留；`library` store 引用全删。）

(c) `onResetProgress` 整函数替换为：

```ts
async function onResetProgress() {
  if (!firstItem.value) return;
  const base = fb.lastFetchedPath;
  if (fb.rootPath === null || base === null) {
    emit('close');
    return;
  }
  // 书 = 阅读目录：目录项拼 entry.path（相对 lastFetchedPath）；图片/文件项直接用所在目录
  const absPath = firstItem.value.isDirectory
    ? PathUtils.join(base, firstItem.value.path)
    : base;
  try {
    const descriptor: SourceDescriptorLocal = { type: 'local', rootPath: fb.rootPath };
    const ok = await resetProgressByLocation(descriptor, absPath);
    if (!ok) log('[RowContextMenu] resetProgressByLocation no-op', absPath);
    await readStatus.refresh();
  } catch (e) {
    log('[RowContextMenu] resetProgressByLocation failed', e);
  }
  emit('close');
}
```

- [ ] **步骤 4：运行验证通过**

```bash
npx vitest run src/components/filebrowser/RowContextMenu.test.ts
```

预期：全部 PASS（既有用例不回归——regenerate/read-now 等不涉及 library store）。

- [ ] **步骤 5：Commit**

```bash
git add src/lib/tauri.ts src/components/filebrowser/RowContextMenu.vue src/components/filebrowser/RowContextMenu.test.ts
git commit -m "fix(filebrowser): 右键重置阅读进度按位置精确重置（清 finished+位置）

旧实现查 favorites 列表 + 只比 rootPath：非收藏书落空、同根多本认错人。
新单命令 resetProgressByLocation(descriptor, absPath)；目录项 join、图片项用所在目录"
```

---

### 任务 6：E 前端——喜欢状态表 + 三入口二态 toggle

**文件：**
- 修改：`src/lib/tauri.ts`（setFavorite 附近）
- 修改：`src/components/filebrowser/FileBrowser.vue`（script + 工具栏 817-830 + ctx/detail 绑定）
- 修改：`src/components/filebrowser/EntryDetailPanel.vue`（~165 按钮）
- 修改：`src/components/filebrowser/RowContextMenu.vue`（~149 按钮）
- 修改：`src/locales/zh-CN.ts` / `en-US.ts`（reader 段）
- 修改：`src/components/filebrowser/FileBrowser.test.ts`

- [ ] **步骤 1：写失败测试（FileBrowser.test.ts）**

(a) vi.mock 工厂追加三项（缺 export 会让新 import 变 undefined）：

```ts
  setFavorite: vi.fn(async () => undefined),
  getBookStatus: vi.fn(async () => null),
```

并 import：`import { setFavorite, getBookStatus } from '@/lib/tauri';`

(b) 新 describe（沿用该文件既有的挂载/选目录模式；若文件有 `mountBrowser` 类辅助则复用）：

```ts
describe('工具栏喜欢按钮二态 toggle（module3.0.14）', () => {
  it('选中已喜欢目录 → 显示已喜欢，点击调 setFavorite(false)', async () => {
    vi.mocked(getBookStatus).mockResolvedValueOnce({ bookId: 7, isFavorite: true });
    const w = await mountWithEntries([dirEntry, imgEntry]); // 既有辅助或按既有用例挂载
    fbStore.selectSingle(dirEntry);
    await flushPromises();

    const btn = w.find('[data-test="btn-add-to-library"]');
    expect(btn.text()).toContain('已喜欢');
    await btn.trigger('click');
    await flushPromises();
    expect(setFavorite).toHaveBeenCalledWith(7, false);
  });

  it('选中未喜欢目录 → 显示＋喜欢，点击走 addToLibrary 现流程', async () => {
    vi.mocked(getBookStatus).mockResolvedValueOnce({ bookId: 9, isFavorite: false });
    const w = await mountWithEntries([dirEntry, imgEntry]);
    fbStore.selectSingle(dirEntry);
    await flushPromises();

    const btn = w.find('[data-test="btn-add-to-library"]');
    expect(btn.text()).not.toContain('已喜欢');
    await btn.trigger('click');
    await flushPromises();
    expect(setFavorite).not.toHaveBeenCalled();
    expect(mockedCreateBook).toHaveBeenCalled(); // addToLibrary 现流程
  });

  it('竞态（审查 P1）：in-flight 查询在喜欢之后才返回 null，不得覆盖已喜欢终态', async () => {
    let resolveStatus!: (v: { bookId: number; isFavorite: boolean } | null) => void;
    vi.mocked(getBookStatus).mockReturnValueOnce(new Promise((r) => { resolveStatus = r; }));
    vi.mocked(mockedCreateBook).mockResolvedValueOnce(42);
    const w = await mountWithEntries([dirEntry, imgEntry]);
    fbStore.selectSingle(dirEntry); // 触发 ensureLikeState，查询挂起
    await new Promise((r) => setTimeout(r, 0)); // 等 watch 触发、IPC 已发出

    await w.find('[data-test="btn-add-to-library"]').trigger('click'); // 喜欢 → 写终态
    await flushPromises();
    expect(w.find('[data-test="btn-add-to-library"]').text()).toContain('已喜欢');

    resolveStatus(null); // 旧查询此刻才返回 null
    await flushPromises();
    // epoch 守卫生效：旧结果作废，终态不被覆盖
    expect(w.find('[data-test="btn-add-to-library"]').text()).toContain('已喜欢');
  });
});
```

（`mountWithEntries` / `dirEntry` / `imgEntry` / `fbStore` 按该文件既有测试的构造方式实现——mock `listDirectory` 返回 entries、mount FileBrowser、flushPromises、从 `useFileBrowserStore()` 拿 store。）

- [ ] **步骤 2：运行验证失败**

```bash
npx vitest run src/components/filebrowser/FileBrowser.test.ts
```

预期：新用例 FAIL（按钮永远显示「＋ 喜欢」/ getBookStatus 未被调用）。

- [ ] **步骤 3：实现**

(a) `tauri.ts` 在 `setFavorite` 后加：

```ts
/** module3.0.14：按位置查单本书喜欢状态（不筛 favorite、无分页）。 */
export interface BookStatus {
  bookId: number;
  isFavorite: boolean;
}
export async function getBookStatus(
  descriptor: SourceDescriptor,
  absPath: string,
): Promise<BookStatus | null> {
  return invoke<BookStatus | null>('get_book_status', { descriptor, absPath });
}
```

(b) `FileBrowser.vue` script（`selectedEntry` computed 之后）加：

```ts
// module3.0.14 spec E：按 locationKey 缓存的喜欢状态（工具栏/详情面板/右键菜单三入口共用）
// locationKey = descriptorId + '|' + absPath —— 仅 absPath 会跨源串状态（不同源都可能有 vol1 / ''）
interface LikeState { bookId: number; isFavorite: boolean }
const likeStateByLocation = ref(new Map<string, LikeState | null>()); // null = 已查无书
const pendingLikeLookups = new Set<string>();
// 审查 P1 竞态守卫：toggle 写状态时 bump epoch；in-flight 旧查询返回时 epoch 失配即作废
const likeLookupEpoch = new Map<string, number>();

function locationFor(entry: MediaEntry): { key: string; descriptor: SourceDescriptorLocal; absPath: string } | null {
  if (!entry.isDirectory || fb.rootPath === null || fb.lastFetchedPath === null) return null;
  const descriptor: SourceDescriptorLocal = { type: 'local', rootPath: fb.rootPath };
  const absPath = PathUtils.join(fb.lastFetchedPath, entry.path);
  return { key: `${descriptorId(descriptor)}|${absPath}`, descriptor, absPath };
}

async function ensureLikeState(entry: MediaEntry): Promise<void> {
  const loc = locationFor(entry);
  if (!loc || likeStateByLocation.value.has(loc.key) || pendingLikeLookups.has(loc.key)) return;
  pendingLikeLookups.add(loc.key);
  const epoch = (likeLookupEpoch.get(loc.key) ?? 0) + 1;
  likeLookupEpoch.set(loc.key, epoch);
  try {
    const st = await getBookStatus(loc.descriptor, loc.absPath);
    if (likeLookupEpoch.get(loc.key) === epoch) {
      likeStateByLocation.value.set(loc.key, st ? { bookId: st.bookId, isFavorite: st.isFavorite } : null);
    }
  } catch (e) {
    log('[FileBrowser] getBookStatus failed', e);
    if (likeLookupEpoch.get(loc.key) === epoch) likeStateByLocation.value.set(loc.key, null);
  } finally {
    pendingLikeLookups.delete(loc.key);
  }
}

// 切目录清空（键含 descriptor 身份，清空只为控制规模）
watch(() => fb.lastFetchedPath, () => likeStateByLocation.value.clear());
// 选中目录 → 查询
watch(() => (selectedEntry.value?.isDirectory ? selectedEntry.value : null), (e) => {
  if (e) void ensureLikeState(e);
});

const selectedLike = computed<LikeState | null | undefined>(() => {
  const loc = selectedEntry.value ? locationFor(selectedEntry.value) : null;
  if (!loc) return undefined;
  return likeStateByLocation.value.get(loc.key); // undefined = 未查到（默认显示＋喜欢）
});

async function onToggleLike(entry: MediaEntry): Promise<void> {
  const loc = locationFor(entry);
  if (!loc) return;
  const st = likeStateByLocation.value.get(loc.key);
  if (st?.isFavorite) {
    try {
      await setFavorite(st.bookId, false);
      likeLookupEpoch.set(loc.key, (likeLookupEpoch.get(loc.key) ?? 0) + 1);
      likeStateByLocation.value.set(loc.key, { bookId: st.bookId, isFavorite: false });
    } catch (e) {
      log('[FileBrowser] unlike failed', e);
    }
    return;
  }
  // addToLibrary 返回 bookId——成功后直接写终态，不回查（回查会被 in-flight 旧查询竞态污染）
  const bookId = await readerActions.addToLibrary(entry);
  if (bookId !== null) {
    likeLookupEpoch.set(loc.key, (likeLookupEpoch.get(loc.key) ?? 0) + 1);
    likeStateByLocation.value.set(loc.key, { bookId, isFavorite: true });
  }
}

function onToggleLikeClick() {
  if (selectedEntry.value) void onToggleLike(selectedEntry.value);
}
function onToggleLikeFromCtx(entry: MediaEntry) {
  void onToggleLike(entry);
}
```

imports 补：`setFavorite, getBookStatus`（from '@/lib/tauri'）、`descriptorId`（from '@/lib/sourceDescriptor'）、`PathUtils`（from '@/lib/path'，若未引入）。

(c) 工具栏按钮（817-830）改：`@click="onToggleLikeClick"`，文案行改：

```vue
          {{ selectedLike?.isFavorite ? t('reader.liked') : t('reader.like') }}
```

（disabled 逻辑 `canAddToLibrary` 不变。）

(d) EntryDetailPanel：按钮（~165）emit 从 `'add-to-library'` 改 `'toggle-like'`，文案行改二态（新 prop）：

```vue
        {{ likeFavorite ? t('reader.liked') : '＋ ' + t('reader.like') }}
```

props 加 `likeFavorite?: boolean`；emits `'add-to-library'` 改名 `'toggle-like'`。FileBrowser 模板对应绑定（~997）改：

```vue
          @toggle-like="onToggleLikeClick"
          :like-favorite="!!selectedLike?.isFavorite"
```

(e) RowContextMenu：props 加 `likeFavorite?: boolean`；目录「＋ 喜欢」按钮（~149）emit 改 `('toggle-like', firstItem)`，文案：

```vue
        {{ likeFavorite ? t('reader.liked') : '＋ ' + t('reader.like') }}
```

emits `'add-to-library'` 改 `'toggle-like'`。FileBrowser 绑定改 `@toggle-like="onToggleLikeFromCtx"` + `:like-favorite="!!ctxLikeFavorite"`；`ctxLikeFavorite` computed：

```ts
const ctxLikeFavorite = computed(() => {
  const e = ctxMenu.value?.entry ?? null;
  if (!e) return false;
  const loc = locationFor(e);
  return !!loc && !!likeStateByLocation.value.get(loc.key)?.isFavorite;
});
```

打开右键菜单的 handler 里（设置 ctxMenu 后）加 `if (entry.isDirectory) void ensureLikeState(entry);`。

(f) i18n：`zh-CN.ts` reader 段 `like:` 旁加 `liked: '已喜欢',`；`en-US.ts` 对应加 `liked: 'Liked',`。

- [ ] **步骤 4：运行验证通过**

```bash
npx vitest run src/components/filebrowser/FileBrowser.test.ts src/components/filebrowser/EntryDetailPanel.test.ts src/components/filebrowser/RowContextMenu.test.ts
```

预期：全部 PASS（EntryDetailPanel/RowContextMenu 既有 add-to-library 断言需同步改为 toggle-like——按失败信息逐个改）。

- [ ] **步骤 5：type-check + Commit**

```bash
npm run type-check
git add src/lib/tauri.ts src/components/filebrowser/FileBrowser.vue src/components/filebrowser/EntryDetailPanel.vue src/components/filebrowser/RowContextMenu.vue src/locales/zh-CN.ts src/locales/en-US.ts src/components/filebrowser/FileBrowser.test.ts src/components/filebrowser/EntryDetailPanel.test.ts src/components/filebrowser/RowContextMenu.test.ts
git commit -m "feat(filebrowser): 喜欢按钮完整 toggle（三入口二态一致）

getBookStatus 按位置精确查（不筛 favorite 无分页）；FileBrowser 统一状态表
以 descriptorId+absPath 为键防跨源串状态；已喜欢可就地取消（setFavorite false）"
```

---

### 任务 7：H 菜单二合一——删「重试缩略图」死链

**文件：**
- 修改：`src/components/filebrowser/RowContextMenu.vue` + `.test.ts`
- 修改：`src/components/filebrowser/FileBrowser.vue`
- 修改：`src/components/filebrowser/FileList.vue`
- 修改：`src/components/filebrowser/MasonryView.vue` + `.test.ts`
- 修改：`src/composables/useMasonryThumbnails.ts`
- 修改：`src/locales/zh-CN.ts` / `en-US.ts`

- [ ] **步骤 1：写失败测试（RowContextMenu.test.ts 追加负向用例）**

```ts
  it('module3.0.14：图片右键不再渲染 retry-thumbnail 项（二合一，只留 regenerate）', async () => {
    const w = await mountCtx(imgEntry());
    expect(w.find('[data-test="retry-thumbnail"]').exists()).toBe(false);
    expect(w.find('[data-test="regenerate-thumbnail"]').exists()).toBe(true);
  });
```

- [ ] **步骤 2：运行验证失败**

```bash
npx vitest run src/components/filebrowser/RowContextMenu.test.ts
```

预期：新用例 FAIL（retry 项仍存在）。

- [ ] **步骤 3：删除链路（自上而下）**

(a) `RowContextMenu.vue`：删 `onRetry` 函数、Emits 里 `(e: 'retry', ...)`、模板 retry-thumbnail 按钮整块（含注释）；文件头注释「regenerate/retry 显示 N 张」改为「regenerate 显示 N 张」。
(b) `FileBrowser.vue`：删 `@retry="onRetryFromCtx"` 绑定与 `onRetryFromCtx` 函数。
(c) `FileList.vue`：删 `retryBatch` 函数与 defineExpose 中的 `retryBatch,`。
(d) `MasonryView.vue`：171 行 destructure 删 `retryBatch: retryBatchFn`；396 行 expose 删 `retryBatch: retryBatchFn,`。
(e) `useMasonryThumbnails.ts`：删 `retryBatch` 函数、接口 `retryBatch: (paths: string[]) => void;`、返回对象里的 `retryBatch`（`retry`/`regenerate`/`regenerateBatch` 保留——失败卡片 ↻ 按钮与 regenerate 菜单在用）。
(f) `MasonryView.test.ts:138` 删 mock 里的 `retryBatch: vi.fn(),`。
(g) i18n：`zh-CN.ts` 删 `retryThumbnail:` / `retryThumbnailN:` 两行；`en-US.ts` 删对应两行。

- [ ] **步骤 4：运行验证通过 + 全量**

```bash
npx vitest run src/components/filebrowser/RowContextMenu.test.ts src/components/filebrowser/MasonryView.test.ts src/components/filebrowser/FileList.test.ts
npm run type-check
```

预期：全 PASS / 0 error（type-check 会抓住任何漏删的引用）。

- [ ] **步骤 5：Commit**

```bash
git add -A src/components/filebrowser src/composables/useMasonryThumbnails.ts src/locales
git commit -m "refactor(filebrowser): 右键菜单缩略图二合一——删「重试缩略图」项

regenerate（删缓存重建）功能上完全覆盖 retry（不删缓存重排队）；
失败态原位 ↻ 按钮保留。死转发链（RowContextMenu→FileBrowser→FileList→
MasonryView→composable retryBatch）与 i18n key 一并清理"
```

---

### 任务 8：I 瀑布流选中描边环（spec I）

**文件：**
- 修改：`src/components/filebrowser/MasonryRow.vue`（style 84-99 行区）
- 修改：`src/components/filebrowser/MasonryRow.test.ts`

- [ ] **步骤 1：写失败测试（源码守卫）**

MasonryRow.test.ts 顶部补 `import { readFileSync } from 'node:fs';`，末尾追加：

```ts
describe('选中描边环源码守卫（module3.0.14）', () => {
  it('::after 置顶环替代 outline，pointer-events none 不挡点击', () => {
    const src = readFileSync('src/components/filebrowser/MasonryRow.vue', 'utf-8');
    expect(src).toContain('.masonry-row::after');
    expect(src).toContain('pointer-events: none');
    expect(src).toContain('z-index: 3');
    expect(src).not.toContain('outline-offset'); // outline 方案已废弃
  });
});
```

- [ ] **步骤 2：运行验证失败**

```bash
npx vitest run src/components/filebrowser/MasonryRow.test.ts
```

预期：守卫用例 FAIL（现实现是 outline）。

- [ ] **步骤 3：实现（样式替换）**

`.masonry-row` 规则中删 `transition: outline 80ms ease-out;`；注释块与 `.is-selected` 规则整体替换为：

```css
.masonry-row {
  cursor: pointer;
  /* 移除 border + border-radius: hGap/vGap 唯一控制间距, gap=0 时图片无缝拼接。
     选中态用 ::after 置顶环 (module3.0.14): 3.0.7 缩略图卡片层 (absolute inset 0)
     铺满卡片后 outline 绘制层级不可靠, ::after + z-index 确定性盖顶。 */
  overflow: hidden;
  background: var(--color-surface-1);
  contain: layout style;
}
.masonry-row::after {
  content: '';
  position: absolute;
  inset: 0;
  border: 2px solid var(--color-accent);
  pointer-events: none; /* 不挡点击/双击/右键/角标 */
  opacity: 0;
  z-index: 3; /* 高于缩略图卡片层与状态/阶段角标 */
  transition: opacity 80ms ease-out;
}
.masonry-row.is-selected::after { opacity: 1; }
```

- [ ] **步骤 4：运行验证通过**

```bash
npx vitest run src/components/filebrowser/MasonryRow.test.ts
```

预期：全部 PASS（既有 `selected -> is-selected class` 用例不受影响）。

- [ ] **步骤 5：实机验证（devtools）**

`npm run tauri:dev` 起实机（9222 调试端口流程见 `docs/tauri-devtools-debugging.md`）：
1. 打开有图目录切瀑布流，单击一张图 → 卡片出现蓝色描边环（修复前确认无框，记录根因观察）。
2. 点选中的卡片 → 详情面板仍更新（`pointer-events:none` 不挡）。
3. 双击进 reader、右键菜单、失败角标点击均正常。

预期：描边可见 + 交互无回归。

- [ ] **步骤 6：Commit**

```bash
git add src/components/filebrowser/MasonryRow.vue src/components/filebrowser/MasonryRow.test.ts
git commit -m "fix(masonry): 选中描边环改 ::after 置顶实现，恢复选中可视反馈

3.0.7 缩略图卡片层 (absolute inset 0) 盖满卡片后 outline 绘制层级不可靠；
::after + z-index 3 + pointer-events none 确定性盖顶且不挡交互"
```

---

### 任务 9：F ease-out 清理（spec F）

**文件：**
- 修改：`src/components/filebrowser/FileIcon.vue:65`
- 修改：`src/components/filebrowser/FileBrowser.vue:1091`
- 修改：`src/components/filebrowser/FileList.vue:520`

- [ ] **步骤 1：三处替换**

每处把 `var(--ease-out)` 替换为 `ease-out`（CSS 关键字）：

```
FileIcon.vue:65    transition: filter 120ms var(--ease-out);
                → transition: filter 120ms ease-out;
FileBrowser.vue:1091  transition: background 120ms var(--ease-out), color 120ms var(--ease-out);
                    → transition: background 120ms ease-out, color 120ms ease-out;
FileList.vue:520   transition: color 120ms var(--ease-out);
                → transition: color 120ms ease-out;
```

- [ ] **步骤 2：验证无残留 + Commit**

```bash
grep -rn "var(--ease-out)" src/ ; echo "exit=$?"
```

预期：无输出（exit=1）。

```bash
npm run type-check
git add src/components/filebrowser/FileIcon.vue src/components/filebrowser/FileBrowser.vue src/components/filebrowser/FileList.vue
git commit -m "fix(ui): 清理未定义变量 var(--ease-out) 三处，改 CSS 关键字 ease-out"
```

---

### 任务 10：G 文档同步（spec G）

**文件：**
- 修改：`DESIGN.md` §16（1574-1618 行区）
- 修改：`AGENTS.md` 当前状态表（3.0.13 行后）

**CRLF 注意**：两文件均为 CRLF——用**单行锚点 Edit**或 node 补丁脚本，多行 old_string 必失配。

- [ ] **步骤 1：DESIGN.md §16 更新**

- 16.2 删「3.0.3 孤儿清理」行（核实已不存在）；删「设置面板：color_theme 已存 store 但 UI 未暴露」行。
- 16.5 追加一行：`- 颜色主题 UI 暴露（color_theme store 已有，4 套色板 CSS 未实现——暴露即死 UI，与主题切换一并做）`。
- 16.2/16.3 中本包完成项（webdav、cargo test CI、useCrossVolume flag、重置进度、喜欢联动、ease-out）划掉或删除；「跨卷 prev 入口」保留。
- 16.4 顺序表同步（已完成项移除）。

- [ ] **步骤 2：AGENTS.md 状态表追加 3.0.14 行**

3.0.13 行后追加（单行锚点 Edit，锚 3.0.13 行首标签）：

```markdown
| 3.0.14 | 小件打包（9 项：CI cargo test / webdav 修复 / 跨卷竞态 / 重置进度 / 喜欢 toggle / ease-out / 菜单二合一 / 选中环 / 文档同步） | ✅ `v0.1.0-module3.0.14`（spec：`docs/superpowers/specs/2026-08-16-module3.0.14-small-fixes-design.md`）：A verify.yml 补 cargo test；B parse_propfind local_name + Empty/展开式 collection（双用例）；C useCrossVolume identity-null 早退消费 flag；D reset_progress_by_location（descriptor+归一路径精确匹配，清 finished+page+image_name，图片项书=所在目录）；E get_book_status + FileBrowser 状态表（descriptorId+absPath 键）三入口完整 toggle；F ease-out 三处清理；H 右键删「重试缩略图」死链（↻ 原位保留）；I 选中描边环改 ::after 置顶。前端 937→~946，Rust 317→~321。 |
```

（数字以实测为准回填。）

- [ ] **步骤 3：Commit**

```bash
git add DESIGN.md AGENTS.md
git commit -m "docs: 状态同步 module3.0.14（DESIGN §16 盘点修订 + AGENTS 状态表）"
```

---

### 任务 11：全量验证 + tag + push

- [ ] **步骤 1：全量验证**

```bash
npm run type-check && npm test -- --run
cd src-tauri && cargo test
```

预期：type-check 0 error；前端测试 0 fail（基线 937 → 约 946）；Rust 0 fail（317 → 321）。

- [ ] **步骤 2：实机冒烟（可选但推荐）**

`npm run tauri:dev`：瀑布流选中框 / 右键重置 / 喜欢 toggle / 右键菜单无重试项，四点过一遍。

- [ ] **步骤 3：tag + push**

```bash
git tag v0.1.0-module3.0.14
git push github main
git push github v0.1.0-module3.0.14
```

- [ ] **步骤 4：推送后盯 CI**

verify.yml 现在含 cargo test——确认 GitHub Actions 首跑全绿（webdav 修复保证不红）。

---

## 自检记录

- **规格覆盖度**：spec A→任务2，B→任务1，C→任务3，D→任务4+5，E→任务4+6，F→任务9，G→任务10，H→任务7，I→任务8；「不做清单」无对应任务（正确）；交付/验证→任务11。§3 I 实机验证→任务8 步骤5。无遗漏。
- **占位符扫描**：任务 6 步骤 1 的 `mountWithEntries` 标注「按该文件既有测试的构造方式实现」——这是对既有辅助的引用而非占位；执行者须先读 FileBrowser.test.ts 既有用例复用其挂载模式。
- **类型一致性**：`BookStatus`（Rust camelCase ↔ TS interface）镜像；`find_library_id_by_location` / `reset_progress_by_location_inner` / `get_book_status_inner` 命名在任务 4-6 间一致；`locationFor` 返回 `{key, descriptor, absPath}` 三字段贯穿任务 6。
